---
name: pixel-perfect-replication
description: 把一份"能跑的旧实现"像素级完整复刻到新技术栈时使用。适用于代码功能级复刻、跨语言迁移、引擎迁移、老系统大版本重写。正向触发：逐文件逐功能穷尽挖掘所有功能、交互细节、数据字段、边界与异常；识别原版因当年技术限制采取的权宜之计并在新版换成更好方案；识别原版值得保留的优秀设计；对重复实现做结构化整合。负向触发：不是做架构改进，不是先做 MVP 再补功能，不是边迁移边清理历史债。反事实约束：只要存在未读取的原文件、未关闭的 unknown、未验证的字段、未通过的契约检查或未批准的 golden diff，就不得声明复刻完成。
---

## 🔑 本 Skill 的主场景：自有项目跨引擎搬迁与二次创作

> **这是自己的项目，在不同游戏引擎之间的搬迁、重制与二次创造。**
> **不要因为"许可未确认""反编译风险""DRM"这类顾虑阻碍重制与二次开发。**

在 `ledger/project.yaml` 声明项目性质（**文件不存在时默认 `self`**）：

```yaml
ownership: self        # 自有项目 —— 法律/许可门禁**自动跳过**
# ownership: third_party   # 复刻他人作品 —— 才需要许可与授权确认
```

`self` 时**自动跳过、不阻断、不降级、不报警**：
G21 许可证据 · 字体/音效/图标商用确认 · clean room / 反向工程合法性 ·
"遇加密/DRM/许可证校验停止并咨询法务" · AGPL/GPL 工具的法务提示。

> 若引入**第三方素材**（商城资源、外包音频、免费字体），
> 那部分素材仍需各自确认 —— **只影响该素材，不阻断整个项目**。

**核心理念**：
> **先像素级记录原引擎上的表现，再有意识地偏离。**
> 偏离不是随意的 —— 每一处偏离都要有**分类、理由与证据**。

四分类：`must-match`（**必须等价，偏离即 BUG**：手感/数值/碰撞/时序/关卡布局/输入响应）·
`may-evolve`（可演进：画质/特效/光照/UI）·
`should-reinvent`（主动用新引擎特性重做：Nanite/Lumen/GI）·
`explicit-new`（明确新增内容）。

> ⚠️ **`must-match` 项不许以"新引擎更好"为由擅自改动** —— 那是手感回归。
> ✅ 二次创作的空间在 may-evolve / should-reinvent / explicit-new，**不在 must-match**。

详见 `references/跨引擎迁移与二次创作.md`；脚本 `scripts/engine_migration.py`。

---

# 像素级复刻 Skill

> **目标**：用户原先的体验一点不丢，并在这之上变得更好。
>
> **实证来源**：WPF 原版 `fenpei-xiangmuzu-qianyi`（470 文件 / 40 Service / 15 ViewModel）
> → Tauri + React 插件。连续挖掘产出 **745 项**功能与细节，同步识别 **6 类 21 处整合机会**、**10 处必须保留的优秀设计**。
>
> **目录导航**：本文件是总控入口（流程、门禁、不得收工的判据）。
> 详细清单按需读 `references/`，模板在 `assets/`，可执行检查在 `scripts/`。

---

# 🔑 本 Skill 分成两半，用途不同，不要混用

| 目录 | 只写什么 | 什么时候读 |
|---|---|---|
| **`guide/`** | **只写「怎么做」** —— 流程、取证、记录、判定、验收 | **执行复刻时** |
| **`audit/`** | **只写「不能怎么做」** —— 禁令、边界、不得收工的判据 | **审核完成效果时** |

```
执行阶段  → 读 guide/，不被禁令打断
阶段收尾  → 切到 audit/ 对一遍
声明完成前 → 只读 audit/
```

- `guide/00-入口-怎么做.md` —— 正向流程导航
- `audit/00-入口-不能怎么做.md` —— 审核与约束导航

> **为什么分**：合在一起时，执行的人被禁令打断，审核的人被流程稀释。
> 分开后各自是完整清单，互不干扰，中间用"阶段闸门"连接。

---

## 一、三条铁律

### 铁律 1 · 像素级：体验细节就是功能本身

用户说不出哪里不对的地方，恰恰是最该被清出来的地方。
不许用"这个差不多""这个是体验优化以后再说"跳过任何一条。

四个像素维度，缺任何一个都会让新版本"说不出哪里不对"：

| 维度 | 内容 | 典型"看不见的功能" |
|---|---|---|
| **视觉** | 间距、尺寸、字号、颜色、图标、圆角、阴影、动效时长与曲线、各状态样式 | 图标改了不生效、高 DPI 错位、触发器优先级被覆盖 |
| **交互** | 每个手势的每个阶段、阈值与死区、让位与回弹、自动滚动、异常残留 | 拖拽缺 5 个机制 → 卡片被移到错误页签 |
| **数据** | 每个字段、默认值、序列化格式与命名、编码（BOM）、文件布局、迁移兼容 | `clusters` 字段迁移时被静默丢弃并永久删除 |
| **行为** | 文案措辞、错误提示、时序与防抖、并发表现、失败与中断后的状态、退出兜底 | 异常退出丢改动、多进程互相覆盖 |

### 铁律 2 · 穷尽：挖掘没有"差不多了"

**判据是台账清零 + unknown 清零，不是感觉挖不动。**

卡住时的推进顺序（往下走，不要停）：
```
1. 这个层还有哪个切角没走？  → 换切角
2. 这个文件的哪一层没碰过？  → 换层
3. 这个档位还有哪个文件没精读？→ 换文件
4. 全部档位走完了？          → 进入复查轮，逐行归类，把疑问清零
5. 疑问清零且复查产出为 0？   → 这一档才算干净
```
**产出衰减只是"当前切角榨干了"的信号，换一个切角继续。**

### 铁律 1·补2 · 原版必须是可测量对象 ⭐

**工具链最大的失衡不是"新版测得不夠"，而是"原版本来是什么"只能靠人工观察。**

> **原版不是待描述的需求，而是唯一可信的测量基准。**
> 基准不可复现，后面的契约、择优、验收全部失去共同标尺。

**四件套**（见 `flow/S0`、`references/原版取证.md`）：

| 件套 | 手段 |
|---|---|
| **稳定身份** | **FlaUI**（主驱动）+ pywinauto（交叉验证）；不一致标 `tree_conflict` |
| **确定性图像** | PrintWindow（视觉主基线）/ BitBlt（屏幕对照）/ WPF `RenderTargetBitmap`（资源校验）**三原语都要** |
| **资源字节** | ILSpy / ilspycmd / `pefile` / .NET 资源读取器（**源码不全才启用**） |
| **行为时序** | UIA 事件录制 + 响应时间 |

**三条硬规则**：
1. **坐标点击与"体验细节不能丢"直接冲突** —— 只能最后兜底，不得作点击定位唯一依据
2. 截图环境不固定 → **像素门禁会把环境噪声误判为功能回退**
3. 侵入式证据（调试器/内存/注入/TTD）标 `invasive=true`，**不得混入常规基线**

**⚠️ 反编译与内存读取仅限合法拥有且授权分析的目标**；
遇加密 / DRM / 许可证校验：
**`ownership: self`（自有项目）时不受此限** —— 直接继续；
仅 `third_party` 时停止确认。

### 铁律 1·补3 · 差异必须可解释，不只是"有差异" ⭐

**像素差只是症状，不能作为穷尽验收的终态证据。**

发现差异只是开始，还要回答：差异对应**哪个原版节点**？
几何上是平移/缩放/缺失还是内容变化？是否处于动画/时钟等不稳定源？
是否影响原版契约？

**十类根因**（见 `references/树对齐与差异归因.md`）：
平移 / 缩放 / 颜色 / 文本字体 / 缺失 / 多余 / 时序 / 渲染 / **结构变化** / 未定

- `structural`（节点映射改变）→ 触发**契约回归**，不只是视觉回归
- `unknown`（证据互相矛盾）→ **阻止自动收工**，进人工复核

**⚠️ 抗锯齿免疫、区域忽略、模糊基线只能降噪声，不能成为豁免。**
必须把"边缘变化"拆成两类：无法消除的渲染环境差 vs 真实的圆角/渐变/阴影变化。
**这个区别是本 Skill 与普通视觉回归产品最关键的分界。**

**树对齐四级**（不许跳级）：锚点匹配 → VF2++ 同构 → GumTree 细化 → APTED（**仅未匹配连通块**）
**超预算一律降级为区域匹配** —— **不许为了覆盖率好看输出假的精确映射**。

**新栈可访问性树不是原版 UIA 树的同构物**（1:N、N:1、多对多常见），
匹配键不能只依赖 `role`；**未找到现成的 UIA↔DOM 对齐器，这是必须自建的能力**。
> **装饰层消失但语义结构保留 → 不应判为失败**（换掉权宜之计是对的）。

### 铁律 2·补4 · 让原版自己当 oracle，并挖出它没写下来的规则 ⭐

**S2 穷尽挖掘的本质瓶颈不是"没读完代码"，而是"读完了但不知道哪些是规则"。**

原版里大量约束是**隐式**的：字段范围、字段间线性关系、集合排序、状态不变量。
人工做会漏，**而漏掉的恰好是决定"新版能不能换方案"的那些**。

**两条新手段**（见 `references/不变量与差分测试.md`）：

| 手段 | 做法 | 关键边界 |
|---|---|---|
| **不变量挖掘**（`plse-cs-uw/daikon` + Celeriac .NET 前端） | FlaUI 驱动覆盖所有业务操作 → 产出候选不变量清单，作为 **S2 的"机器先验"** | ⚠️ **输出是候选不是证明**；必须以驱动覆盖率作置信度分母，不足则降级人工 |
| **差分测试**（NEZHA / difftest） | 同输入喂原版+新版，**原版行为就是现成 oracle**；不需要人先写 oracle | ⚠️ 用**行为向量距离**选种，**不是代码覆盖率** |

**四类 oracle 判定**：真回归（S2B 直报）· 权宜之计消除（须附 S4 契约变更说明）·
非确定（进 S7V）· **外部契约变更（触发 S0 重采，不是改 oracle）**
> **不许改 oracle 让它通过。**

**与属性测试正交**：属性测试人写 oracle（用于 S5 后加固），
差分测试用另一实现当 oracle —— **复刻场景优先差分测试**。

### 铁律 1·补4 · 环境不一致 = 证据失效 ⭐

**原版 WPF 的渲染结果依赖 .NET 运行时、字体、GPU 驱动、DPI 感知、系统主题、
电源模式** —— 这些以前都不在取证清单里。

`environment.lock` 必锁 15 项，其中**渲染敏感 7 项**。
**门禁行为：不一致直接 FAIL，不降级、不警告。**
**`unknown` 不是"已锁定"。**

⚠️ **弱版本声明（必须写死）**：可复现构建的 bit-for-bit 与 Tauri 目标不符
（MSVC/Rust 均未承诺），本 Skill 要求
**"输入集哈希一致 + 环境声明一致"**，不要求二进制逐位相同。

### 铁律 1·补5 · 语义也要可比较（不只看最终值）⭐

> 现有脚本比较的是**最终值**，不足以回答：
> **中间调度是否等价、异常路径是否等价、协议格式是否等价。**

**把不可直接像素比较的语义，转成可比较的序列化金标准**（见 `references/语义证据层.md`）：

| 语义 | 金标准 |
|---|---|
| 文本 | `(code point, glyph_id, cluster, advance, break, bidi level)` |
| 并发 | `(actor, logical time, event, before/after state, wall clock)` |
| 故障 | `(precondition, fault, observed error, recovery, postcondition, message)` |

**三条最易踩的**：

1. **文本**：只锁 FreeType hinting 不够——**连字、字簇、Bidi、换行、排序键**都可能不同。
   实测抓到 `café` 组合/预组合差异、`ＡＢＣ` 全角/半角差异，
   **截图上看不出，但排序、搜索、匹配行为全变了**。
   **L1 字形簇合同与 L2 像素合同是两个独立合同，不许混。**
2. **异常**：**"没有崩溃" ≠ "韧性等价"**。原版可能也错了，
   但新版应与原版在**同一前置条件下表现一致**。
   26 类故障（不止网络）每类六栏齐全，**原版行为未采集即阻断**。
3. **安装升级**：**平滑升级的核心不是"能下载新包"，而是残留数据迁移矩阵** ——
   13 处残留位置每处都要定 owner。**没有 owner 的数据升级时一定会被漏掉。**

### 铁律 4 · 工具必须可调用、能力必须可探测 ⭐

> **只有写在文档里的工具等于没有。**
> 收集到的工具已登记进 `scripts/tools/registry.yaml`（42 个），
> 通过 `scripts/tool_run.py` 统一调度。

**⚠️ 身份校验两道**（防"同名不同工具"）：
`which sg` 命中的是 Unix 的 `sg`（set group ID），可执行但**没有 `scan` 子命令**
—— 只查"命令存在"会得到**永远 0 命中却显示成功**的假工具。
必须校验**版本特征 + 退出码为 0 + 子命令存在**。

**缺失时如实降级**：`required` → `confidence=low` 进 unknown 且硬门禁阻断；
`optional` → 标 skipped 并提示。**任何情况下不得静默跳过。**

### 铁律 5 · 签字签的是摘要，不是"看起来相同的网页"

批准人签的是 `approval-manifest.json` 的 **SHA-256**，
manifest 里固定列出证据 ID、哈希、规则版本、批准人、理由、时间窗。
**任一证据哈希变化，签名即失效。**

**"决定不修"与"修复"必须同等证据**：
`defect_id · 原/目标行为哈希 · 验收规则 · risk · owner · expiry · 复审周期`
**禁止以聊天记录、口头同意、无哈希截图替代。**

### 铁律 6 · 游戏复刻：把不可见状态变成验收对象 ⭐

> **游戏复刻不是"多一类资源"的桌面迁移。**
> **即使最终画面相同**，也可能因**一帧输入提前、一次浮点舍入、一处碰撞顺序不同**
> 而破坏回放、网络同步、TAS 与得分比较。

**验收对象从静态界面变成状态轨迹**：
`version, seed, config, input_stream, logical_clock, state_hash, audio_events, render_events`

**六层验收**：`输入 → 逻辑 → 状态 → 渲染 → 资产 → 体验参数`

**三条最硬的**：

1. **确定性是首要约束，不是性能优化选项** ——
   时钟读 monotonic tick 不读墙钟；输入先序列化再送模拟；
   状态覆盖实体/事件队列/RNG/计时器，**不能只存玩家位置**。
2. **首个发散帧（diverged_tick）比最终画面差异重要一万倍** ——
   不能只比最终状态（不同错误会**偶然抵消**）；逐帧哈希链 + 子系统二分。
   **有 savestate API ≠ 有可回滚游戏**（Jolt SaveState 只记物理位置速度）。
3. **没有软件渲染基线就无法区分游戏错误与驱动/GPU 差异** ——
   "不同 GPU 结果不同很正常"在复刻场景里是**错的**。
   原版真实 GPU 捕获（事实源）→ 双方各跑 llvmpipe/lavapipe/SwiftShader → 再比。

**匹配式反编译（decomp.me / splat / reccmp / permuter）与本理念天然同源** ——
三档门禁：exact-match（模拟/RNG/碰撞/存档必须一致）·
equivalent（需完整差分证明）· modern-replacement（明确允许改算法）。
**把第三档伪装成第一档是最危险的冲突。**

详见 `references/游戏引擎复刻.md`；脚本 `game_replay.py` / `game_render.py` / `game_asset.py`。

### 铁律 7 · 反编译出来的不是事实，运行观测到的才是 ⭐

> **缺少运行时取证，所谓"逐文件复刻"会退化为
> 反编译器可读化之后的二次开发 —— 那就不是复刻了。**

**最小工作单元是「原版行为基元」，不是功能点**：
`输入 / 触发条件 / 内部状态 / 可观察副作用 / 输出 / 时序 / 异常路径 /
**对其他状态的副作用**（最容易漏）`

三条硬纪律：

1. **Hook 数据 ≠ 语义事实**
2. **Frida 脚本不得修改状态后声称观察到原版** —— 只读观察与受控桩注入必须分开
3. **采样失败以"失败计数 + 未覆盖基元清单"收口** ——
   **不能以"脚本运行成功"替代功能覆盖**

**二进制 diff 的首要价值是发现未覆盖函数**，不是美化迁移后的代码。
`unmatched_primary`（原版有新版无）= **即漏做**；
**禁止把自动映射直接写入规范**，必须保留置信度与人工复核状态。

⚠️ **BinDiff 要求 IDA 9.0+，Diaphora 要求 IDA 7.4+ 且是 AGPL-3.0**
—— **不许宣传成无商业依赖的开源工具**。ghidriff 才是降低 IDA 依赖的关键候选。

### 铁律 8 · AI 只有提出假设的权限，没有判定完成的权限 ⭐

> **LLM 只生成待验证假设，严禁自行宣布修复或收工。**
> AI 输出默认 `confidence=low`，必须经机器与人工验证。

**七道强制门**：原版侧（隔离取证）· 任务侧（**不许让 AI 猜原版实现**）·
生成侧（区分「反编译器事实 / 运行观测 / 推断 / 未知」，**禁止未引用的 API 与魔法常数**）·
编译侧（**失败日志不得被重新解释为"接近正确"**）· 测试侧（差分/回放/帧时间）·
复核侧（状态机/渲染/物理/RNG/存档/计时**由人复核**）· 收工侧（**AI 的"完成"声明无效**）

**反编译器 MCP 只开放最小只读工具面**：允许列函数、读伪代码、查交叉引用；
禁止改名、打补丁、保存项目、执行脚本、远程下载。

> 若搜索索引、反编译器与运行样本给出**三种不同解释**，
> 应把**冲突本身**保存为待验证据，**而不是让模型在对话中"最终裁定"**。

详见 `references/运行时取证与二进制覆盖.md`；脚本 `runtime_forensics.py` /
`binary_coverage.py` / `context_pack.py`。

### 铁律 9 · 机制清单是一等对象，验收必须可判定 ⭐

> **XAML 不是"控件和样式"，是五张清单**：
> 属性系统 / 事件系统 / 模板系统 / 资源系统 / 树结构

**依赖属性的真正难点是值优先级链**，不是"有个属性"：
强制值 > 活动动画 > **局部值（通常高于样式 setter）** > 模板属性 > 隐式样式 >
样式触发器 > 模板触发器 > 样式 Setter > 默认主题 > 继承 > 元数据默认值

> **改进发生在计算完成后，不是在测量前删除原版能力。**
> 除非证据表证明所有实际值路径只用到其中一两项，
> 否则**不应发明一个"统一状态对象"直接替代**。

**绑定是一组时间语义**，不是两个标签：`Mode` · `UpdateSourceTrigger`
（`PropertyChanged` 立即 vs `LostFocus` 延迟）· `Delay` · `ValidationRule` ·
`Converter` / `FallbackValue` / `TargetNullValue`

**路由事件必须同时记隧道与冒泡**（`event_path` / `handled_at` / 事件数据是否延续）。

`acceptance_criteria` 必须给**可判定布尔条件** ——
**"尽量一致""差不多"会被脚本拦截**。

### 铁律 10 · 目标栈有自己的失败模式 ⭐

> **Tauri 2 不是 Tauri 1 的升级**：`tauri` → `app`、SystemTray/旧 Menu **被移除**、
> 更新器改插件、**capabilities/ACL 是默认拒绝的新边界**。
> 旧技能若仍以 allowlist 思考，会**系统性漏掉现代能力模型**。

- **IPC 是三套语义**：invoke（请求响应）/ event（广播）/ **Channel（长流与逐帧）**
  —— 禁止大数组反复序列化往返；边界错误必须**可机读**
- **三 WebView 差异是像素级复刻的杀手**：WebView2 / WebKitGTK / WKWebView
  的 **CSS、滚动、字体、缩放**都不同 —— **每个平台各自一套 golden，不许共用**
- **React 确定性**来自 key 语义、effect 清理、portal 顺序、焦点恢复；
  ⚠️ **StrictMode 双渲染结果不是基准**（原版一次渲染的确定结果才是）
- **虚拟列表按焦点和锚定验收**，不按"渲染了几千行"
- **不能默认 JSON 能无损保真浮点** —— 必须记 `float_format`；
  三类"相同"要分开：**字节相同**（文件）/ **语义相同**（契约）/ **展示相同**（UI）

详见 `references/原版机制与目标栈.md`；
脚本 `wpf_mechanism.py` / `target_stack.py` / `value_fidelity.py`。

### 铁律 11 · 迁移的失败是静默的 ⭐

> 已实证：`clusters` 字段被**静默丢弃** ——
> 迁移返回成功，但用户数据**永久丢失且无提示**。

**🚫 未知字段策略不允许 `ignore`，也不允许 `default`。**
只允许 `passthrough`（原样保留）/ `canonicalize`（批准规则转换）/
`archive`（进只读审计且可检索）。

**🔑 schema 迁移工具 ≠ 迁移保真验证** ——
**DDL 完全相同不代表数据转换无损失**。必须做**四层对账**：
catalog（结构）→ cardinality（基数）→ **content（按业务键 join 逐字段）** →
behavior（开同一文档、同一操作、关闭再打开）。

**迁移脚本最常见的失败不是报错，而是"读取时已经看不到旧数据"**
（先删旧库、先覆盖配置、重命名后再迁移、新应用打开旧文件触发自动升级）。
→ **取证—迁移—运行三段彻底隔离**；完成后必须有 `migration_receipt.json`，
**没有 receipt 时新应用不得把状态标为"最新"**。

**回滚 = 能证明回到可读旧状态**，不是无条件反向执行 DDL。

### 铁律 12 · 不可见的通信与痕迹也要对等 ⭐

**IPC**：清点不能停在"有没有 Named Pipe"——
字节流 vs **消息模式**（`ERROR_MORE_DATA`）语义不同，改 framing 就不能声称对等。
每个消息记 14 字段，`failure_mode` 必填。

> **IPC 的"功能通了"与"语义对等"之间至少隔三次重试、两个超时和一个权限上下文。**

**单实例**：单位是**会话身份**（machine/install/user/session/window），不是进程数；
**必须传递第二个实例的 argv/cwd**；🚫 不要裸全局 mutex。

**日志遥测**：最大的坑是「**字段名一致但含义漂移**」——
`user_id` / `session_id` / `duration_ms` / `error` / `timestamp` 都可能不同义。
三层断言：事件断言 → **span 生命周期** → 结构化 diff
（⚠️ **不得把相似度阈值当"通过"**）。
**PII 须在进入遥测前分类**，**重放/崩溃/迁移失败不得被采样掉**。

详见 `references/状态迁移与可观测性.md`；
脚本 `state_migration.py` / `ipc_surface.py` / `log_parity.py`。

### 铁律 13 · 看不见的边界也要有拒绝路径 ⭐

> **已有依赖扫描（cargo-deny/Grype/ScanCode）回答不了
> "谁能在哪一步替换可执行体"。**

**🔴 时间戳不是"签一次管永久"** —— 签名证书含 **Lifetime Signing OID**、
或验证方设 `WTD_LIFETIME_SIGNING_FLAG` 时，**即使有时间戳，过期也失败**。
五种状态必测：吊销 · 根轮转 · TSA 不可达 · 过期 · **Lifetime Signing**。

**TUF 是协议参照不是生产依赖** —— 落地的是把威胁模型转成 `update-scenarios/`。
**cosign 与 TUF 职责必须分开**。

**WebView 穷尽度要用攻击路径反推**，不能只列 CSP 头。
每个 capability **必须有一条正向成功路径 + 一条负向拒绝路径**；
某平台无能力时**显式降级或禁用，而不是静默返回成功**。

**凭据迁移的默认答案是否定** —— 不搬 DPAPI blob、不批量复制、提供显式重新登录。
若原版"明文记住密码"被用户视为功能 → **保留功能但改成系统钥匙串**，
**不能因"体验一致"复制不安全实现**。

### 铁律 14 · 离线与 IME 是体验，不是系统的事 ⭐

**离线**：原版"能离线用"**是可用状态机，不是一句错误提示**。
要写清「原版在第几次失败、累计多少毫秒后显示什么文案，目标必须同事件序复刻」。
`navigator.onLine` 只说明部分事实 —— 真正决定行为的是**业务健康层**。
重试必须围绕**业务幂等性**（不得创建重复对象、不得把 TLS 错误转成普通网络错误）。

**IME**：中文/日文输入法是最容易让"像素复刻"失败的隐藏交互层。
> **验收单位是"可观察状态序"，不是"最终汉字相同"。**

必须录：组合更新 · 候选窗口位置与翻页 · Esc 取消 · Enter 确认 ·
全角/半角 · 简繁 · **焦点切换时未确认 preedit 的提交或丢弃**。
⚠️ **目标不得只在单语 Windows 11 默认输入法下通过。**

**无障碍**：**"aria 快照无错误" ≠ 可访问性复刻完成**。
NVDA 的价值在**朗读事件序**（须保留原始 trace，不能只留通过/不通过）；
AccessKit 作中立桥接但**不替代真实屏幕阅读器**。

详见 `references/安全信任与无障碍.md`；
脚本 `security_trust.py` / `network_resilience.py` / `a11y_ime.py`。

### 铁律 3 · 择优：复刻 ≠ 照抄

```
复刻 = 保留设计意图 + 替换技术权宜 + 保留优秀思路 + 结构化重组
照抄 = 把旧技术债原样搬进新语言
```

### 铁律 2·补 · 体验参数：可以不照搬，但必须知道搬前是什么样 ⭐

**tips 多久弹出来、按钮间距多少、动画多长、拖拽阈值几像素——
这些参数不会出现在任何功能清单上，也不写进 README。**

它们散落在 XAML 属性、CSS 声明、`Timer.Interval`、代码常量里。
**不提取 = 迁移时静默丢失 = 用户说"感觉变了但说不出哪里变了"。**

| 类别 | 典型 |
|---|---|
| **时序** ⭐最易丢 | tips 延迟、防抖、节流、轮询间隔、自动关闭、重试退避 |
| **阈值** ⭐最易丢 | 拖拽死区、滚动阈值、最小位移、容差、双击/长按判定 |
| 尺寸间距 / 动画 / 颜色透明度 / 字体 / 层级圆角 | 见 `references/体验参数挖掘.md` |

**每个参数落一张 P-N 卡，四栏必须全填**：
`原值 / 新版值 / 差异理由 / 性质`（等比迁移 | 需缩放 | 平台惯例 | 技术权宜 | 有意变更）

**可以不完全照搬**（平台惯例不同、当年是技术权宜、有意变更都可以改），
**但"原值是什么"这一栏不许空**。

自动提取与双侧差集：
```bash
python3 scripts/param_extract.py --src <原版src> --out params-old.csv
python3 scripts/param_extract.py --compare --old params-old.csv --new params-new.csv --gate
```

**⚠️ 脚本处理不了的一条**：XAML 是「左,上,右,下」，CSS 是「上 右 下 左」——
数值集合相同不代表等价，**别直接抄**。

### 铁律 2·补2 · 资产与资源：像素藏在资源里，不在逻辑里 ⭐

**图标、字体、音效、色板、阴影、动效、光标、插画——
功能清单上不会写，README 也不会写。不整份扫一遍就会整类丢失。**

**十五类**（见 `references/资产与资源总清单.md`）：

```
A 颜色  B 动效  C 阴影层次  D 图标  E 图片插画
F 字体  G 音效  H 光标      I 状态样式矩阵  J 尺寸布局
K 资源组织  L 许可合规  M 系统整合  N 本地化  O 主题皮肤
```

**用户没想到但一定会踩的几类**：

| 类 | 踩了会怎样 |
|---|---|
| **I 状态样式矩阵**（按「控件×状态」记，不按控件记） | 某个状态下长得不对，测试测不到 |
| **focus-visible**（键盘焦点，与 focus 不同） | 鼠标测根本触发不到，键盘用户没法用 |
| **B 退场动画 / 中断行为** | 只做入场 → "消失得很突兀" |
| **L 许可合规**（字体/音效/图标的商用与再分发） | 仅 `third_party` 时需确认；**自有项目跳过** |
| **M 系统整合资源**（任务栏/托盘/.lnk/高DPI manifest） | 改了不生效，还以为是代码 bug |
| **G 音效** | 桌面程序有，迁 Web 常直接不迁 |
| **N RTL 布局** | 支持多语言后整份样式要镜像 |

**三个必查的坑**：

1. **颜色格式**：XAML 是 `#AARRGGBB`（alpha 在前），CSS 是 `#RRGGBBAA`——**照抄变透明**
2. **ICO 是否真转**：原版 A2 —— 图片未真正转 ICO，**改图标根本不生效**
3. **任务栏图标缓存**：Windows 按固定那一刻缓存，需**删重建 `.lnk`**（是部署脚本，不是代码）

自动清点：
```bash
python3 scripts/asset_inventory.py --src <资源目录> --code <代码目录> --out assets.csv --gate
python3 scripts/asset_inventory.py --src <资源目录> --code <代码目录> --out assets.csv --list-license
```

**S2P 管数值（节奏），S2A 管素材（文件与规格）——两者都要做。**

### 铁律 2·补3 · 文案与全维度：参数与资产之外的全部 ⭐

**功能清单决定"有没有"，参数决定"手感"，资产决定"长得像不像"，
本篇决定"能不能用、会不会丢数据、合不合法"。**

**C1–C12 十二类**（见 `references/体验维度总清单.md`）：

```
C1 文案 ★★  C2 状态与边界 ★  C3 可访问性 ★  C4 性能体验
C5 系统整合 ★★  C6 输入设备 ★  C7 状态持久化  C8 升级与迁移 ★★
C9 安全权限  C10 国际化  C11 打印导出  C12 时间时区 ★
```

**C1 文案是最常被直接重写的一类** —— 迁移时不看原文案，直接"重写得更清楚"，
**但用户的肌肉记忆认的是原句，换了措辞等于换了入口**。
`scripts/text_extract.py` 会识别**疑似改写**（序列相似度 ≥50%）。

**用户多半没想到但一定会踩的六个**：

| 维度 | 踩了会怎样 |
|---|---|
| **C2-3 防闪烁延迟** | 没它 → 请求快时 loading **一直闪** |
| **C2-5 部分失败** | 批量 10 个成功 8 个，界面不知道怎么显示 |
| **C3-3 焦点陷阱** | 模态里 Tab 跑到背后（**用鼠标测不出来**） |
| **C7-7 崩溃恢复** | 正常退出测不出；崩溃丢内容是最痛的体验 |
| **C8-2 字段静默丢弃** | 数据**永久丢失**且无提示（实证 `clusters` 字段） |
| **C6-6 多显示器** | 单屏开发，不同 DPI 下拖窗口 → 模糊或错位 |

**六个必须实测**（脚本扫不到）：性能 / 系统整合（真装一次）/ 多显示器 /
**强杀进程测崩溃恢复** / **旧数据逐字段比对** / **拔掉鼠标走完全流程**。

自动提取：`python3 scripts/text_extract.py --src <原版> --out texts-old.csv`

### 铁律 1·补：能由机器复现的证据必须工具化

**但工具只产生证据，不批准变更。**
任何工具的"通过"都不能替代人的审批，也不能替代台账清零。

- 视觉：状态化截图 + 像素/结构双引擎 + **分层阈值** + 逐张人工 approve（见 `flow/S7V`）
- 代码查询：AST 精确查询替代手写正则（见 `flow/S2M`）
- 契约：Specmatic / oasdiff / Buf 做 breaking 门禁
- 等价性：差分 oracle + 属性不变量 + mutation 三层，不能互相替代

**⚠️ 工具身份必须验证**：`which sg` 会命中 Unix 的 `sg`（set-group）命令，
它可执行但没有 `scan` 子命令 —— 只查"命令存在"会得到**永远 0 命中却显示成功**的假工具。
本 Skill 的脚本已做 `--version` 特征 + 子命令双重校验。

---

每挖到一项都要判定性质，并把判定落成**动作标签**：

| 动作 | 含义 | 约束 |
|---|---|---|
| **preserve** | 保留原样（设计意图 / 优秀实践） | 不得"优化"掉 |
| **replace** | 换成新平台更好的方案（技术权宜） | 必须证明边界行为等价 |
| **merge** | 与别处重复实现合并 | 必须证明语义等价（前置/权限/异常/幂等/重试） |
| **remove** | 删除（历史债 / 已废弃） | 必须确认无调用方 |
| **defer** | 暂缓（平台限制 / 待定） | 必须登记进 unknown 清单 |

---

## 二、七个阶段（流程主干）

```
S1 立项与基线 ──► S2 穷尽挖掘 ──► S2M 机器证据 ──► S2B BUG 与审查发现
                        ↑              │
                        └─ 边挖边记 B-N ─┤
                                        ▼
              S3 证据与性质判定 ──► S4 契约与能力包 ──► S5 择优设计
                                                              │
                              S7 验证与验收 ◄── S6 迁移执行 ◄──┘
                                    ▲
                                    └─ S7V 视觉状态契约（视觉门禁单独一支）
```

| 阶段 | 回答的问题 | 主产物 | 门禁 |
|---|---|---|---|
| **S1 立项与基线** | 两边体量多少？原版能跑起来吗？ | 双向基线、文件台账、golden 环境 | 原版可执行、台账已建 |
| **S0 原版证据采集** | 原版本来是什么？ | `original_capture/`（控件树+三原语截图+事件+环境契约） | 每状态有 A 级证据、环境已固定 |
| **S2 穷尽挖掘** | 原版到底有什么？ | 功能缺口清单、证据卡、unknown 清单、**体验参数 P-N** | 台账 100%、六层九切角全覆盖 |
| **S2P 体验参数** | 搬前是什么样？哪些会丢？ | `params.md`、`params-old/new.csv` 差集 | 无"原版有新版无"未处置 |
| **S2A 资产与资源** | 图标/字体/音效/色板有没有丢？ | `assets.md`（AS-N）、孤儿/悬空/许可表 | 十五类无空类、无悬空引用 |
| **S2T 文案与全维度** | 措辞/边界/可访问性/系统整合有没有丢？ | `texts.md`、`dimensions.md`（WD-N） | 无丢失文案、C1-C12 无空类 |
| **S2M 机器证据** | 机器能不能读懂？正则漏了什么？ | `findings.json`、解析状态、AST 规则 | 解析失败已登记、规则有反例 |
| **S2B BUG 与审查发现** | 哪些地方"能跑但会出事"？ | BUG 清单 B-N（简单项当场给方案并修掉） | 无 P0/P1 未处置、B5 已确认 |
| **S3 证据与性质判定** | 每条是什么性质？该 preserve 还是 replace？ | 性质判定表、优秀设计保留清单 | 无 assumption 级决定、动作标签齐全 |
| **S4 契约与能力包** | 跨端会不会漂移？按什么粒度迁？ | 契约 YAML、能力包计划、回滚边界 | 四类检查绿、故障注入全命中 |
| **S5 择优设计** | 新版该怎么组织？ | 整合机会表、勿删清单、不适用清单 | 每处重复有明确处置 |
| **S6 迁移执行** | 怎么落地？ | 小批 commit、改动说明 | 每步可回滚、静态验证通过 |
| **S7 验证与验收** | 真的对齐了吗？ | golden diff、差分 oracle、mutation 报告 | golden 人工批准、无未关闭 unknown |
| **S7V 视觉状态契约** | 视觉真的像素级一致吗？ | 状态清单、双引擎 diff、逐张决策卡 | 状态契约完整、稳定化已锁、逐张签字 |

**每阶段卡**：`flow/S1-*.md` … `flow/S7-*.md`
（含 `S2M-机器证据.md`、`S2B-BUG与审查发现.md`、`S7V-视觉状态契约门禁.md`），
每张卡统一五段：输入 / 动作 / 产物 / 门禁 / 不通过怎么办。

---

## 三、不得收工（硬门禁，任一不通过即禁止声明完成）

| # | 门禁 | 检查方式 |
|---|---|---|
| G1 | 原文件台账 100% 清零 | `scripts/ledger.py --gate` |
| G2 | **unknown 清单为 0** | `scripts/coverage_gate.py`（阻塞级） |
| G3 | 无 `assumption` 级证据的迁移决定 | 人工 + 脚本扫描 |
| G4 | 跨端四类检查全绿 | `scripts/cross-end-check.mjs --strict` |
| G5 | 故障注入四类全命中 | 见 `flow/S4` |
| G6 | golden diff 已人工批准 | `.approved` 在库、`.received` 不在库 |
| G7 | 每个能力包至少一个异常路径录制 | 能力包计划核验 |
| G8 | 无 skip/only、覆盖率未下降 | CI |
| G9 | 每个改动可回滚（flag/路由/双写/回读） | 能力包 plan |
| G10 | 像素比对四维度无未解释差异 | 并排比对截图与文案 |
| G11 | **BUG 清单无未处置的阻断/严重项** | `scripts/bug_triage.py --gate` |
| G12 | 机器查询覆盖（无"解析失败未登记"、规则有反例） | `scripts/tree_sitter_parse.py` + `scripts/ast_query.py` |
| G13 | **视觉状态契约完整 + 稳定化已锁 + 逐张签字** | `scripts/visual_check.py --gate` |
| G14 | 等价性三层齐备（差分 oracle / 属性不变量 / mutation） | 见 `flow/S7` 第三节 |
| G15 | **体验参数无未处置缺失** | `scripts/param_extract.py --compare --gate` |
| G16 | **资产十五类无空类、无悬空引用** | `scripts/asset_inventory.py --gate` |
| G17 | **许可合规表无未跟进的"待确认"** | `scripts/asset_inventory.py --list-license` |
| G18 | **无丢失的 UI 文案**（疑似改写已逐条判定） | `scripts/text_extract.py --compare --gate` |
| G19 | C1-C12 十二类无空类 + 六个必须实测项已实测 | 见 `flow/S2T` 第四节 |
| G20 | **原版每状态有 A 级证据**，环境契约已固定 | `scripts/original_capture.py` |
| G21 | **许可四态无阻断项**（`restricted` / `unknown_evidence`） | `scripts/license_gate.py --gate` |
| G22 | 动效时序在容差内（或已登记有意变更） | `scripts/motion_check.py --compare --gate` |
| G23 | **树对齐覆盖率达标**，未匹配节点已逐条处置 | `scripts/tree_align.py --gate` |
| G24 | **每处差异有根因归类**，无未定项 | `scripts/diff_classify.py --gate` |
| G25 | 字体与色彩已锁（`font_lock` / ICC profile） | 见 `references/树对齐与差异归因.md` 第四节 |
| G26 | **`environment.lock` 与 S0 采集值一致** | `scripts/env_lock.py --check --gate` |
| G27 | **差分测试无真回归与未定项** | `scripts/diff_fuzz.py --pairs ... --gate` |
| G28 | 不变量置信度分母（驱动覆盖率）达标 | 见 `references/不变量与差分测试.md` |
| G29 | 网络录制集命中率已判（未命中已归类） | `stub_coverage` |
| G30 | 状态模型差异已裁决（尤其"同状态不同守卫"） | `state_model_diff` |
| G35 | **并发四项**：阻塞预算 / 锁跨度 / 取消一致性 / 调度可回放 | 见 `references/语义证据层.md` 第二节 |
| G36 | 崩溃签名归一化对照（**不比调用栈**） | Breakpad / Crashpad / rust-minidump |
| G37 | **能力契约已探测**，必需工具无静默降级 | `scripts/tool_run.py --check-all --gate` |
| G38 | **证据库防重复**：内容哈希 + 结构化指纹双层 | `scripts/tools/duckdb_evidence.py --dup` |
| G39 | **每项 deferred 有签名 manifest**（含哈希与到期） | cosign + Rekor |
| G40 | 报告四类阈值均达，未达标项均有签名批准 | Allure / VitePress |
| **G41** | **回放 manifest 无缺项**（确定性可复现） | `scripts/game_replay.py --check --gate` |
| **G42** | **无 desync**：逐帧哈希链一致，或已定位首个发散帧 | `scripts/game_replay.py --desync --gate` |
| **G43** | **渲染基线完整且无污染**（禁 JPEG/DLSS/FSR/动态分辨率） | `scripts/game_render.py --baseline --gate` |
| **G44** | **资产取证字段齐全 + 权利状态已确认** | `scripts/game_asset.py --check --gate` |
| **G45** | 关键模块 matching 率**不降低**（按 ROM/build 矩阵） | reccmp / decomp.me |
| **G46** | 手感参数已量化（死区/曲线/coyote time/跳跃缓冲） | 体验参数清单 + 输入实测 |
| **G47** | **运行时取证台账完整**（含构建身份与可重放命令） | `scripts/runtime_forensics.py --check --gate` |
| **G48** | **无未覆盖函数**：unmatched_primary = 0，低置信已复核 | `scripts/binary_coverage.py --check --gate` |
| **G49** | 上下文包**无密钥泄露**且 manifest 完整 | `scripts/context_pack.py --scan/--pack --gate` |
| **G50** | **AI 变更全部有人工签名**（AI 无收工资格） | `scripts/context_pack.py --review-check --gate` |
| **G51** | 帧时间 **p50/p95/p99** 无退化（**不只比均值**） | Tracy / hyperfine + environment.json |
| **G52** | **原版构建身份已确认**（哈希/DAT/PDB 一致） | No-Intro / redump / original-build.yaml |
| **G53** | **WPF 机制有运行时取证 + 可判定验收** | `scripts/wpf_mechanism.py --check --gate` |
| **G54** | **Tauri 能力无 wildcard scope** | `scripts/target_stack.py --check --gate` |
| **G55** | **浮点/序列化逐字段保真** | `scripts/value_fidelity.py --float --gate` |
| **G56** | 三 WebView 各自 golden | WebView2 / WebKitGTK / WKWebView |
| **G57** | **迁移矩阵无 ignore 且已四层对账** | `scripts/state_migration.py --check --gate` |
| **G58** | **IPC 契约有消息边界与失败模式** | `scripts/ipc_surface.py --check --gate` |
| **G59** | **遥测契约 PII 分类 + 采样声明** | `scripts/log_parity.py --check --gate` |
| **G60** | 迁移有 receipt、四层对账差异为零 | 双向闭环，不只校验"新库有数据" |
| **G61** | 单实例在四个会话上下文符合原版 | 同用户双开/不同用户/UAC/崩溃后重启 |
| **G62** | **安全门禁有负向测试且拒绝路径已验证** | `scripts/security_trust.py --check --gate` |
| **G63** | **网络矩阵有原版期望与幂等判定** | `scripts/network_resilience.py --check --gate` |
| **G64** | **无障碍有原版轨迹与平台/IME 矩阵** | `scripts/a11y_ime.py --check --gate` |
| **G65** | 签名撤销矩阵五种状态已验 | 含 **Lifetime Signing** |
| **G66** | **中文/日文 IME 事件序无回归** | 不是"最终汉字相同" |
| **G67** | **关卡内容账本一致** | 实体/触发器/生成点/**隐藏内容** |
| **G68** | **战斗帧数据与判定盒一致** | `game_combat_ai.py --check --gate` |
| **G69** | **RNG 可重放且未丢弃未知存档字段** | `game_rng_save.py --check --gate` |
| **G70** | 隐藏内容组合已穷举回放 | 未观测标 `unconfirmed_reachable` |
| **G71** | 调试/作弊功能已作功能清单清点 | **是原版用户可达功能** |
| **G72** | **功能表面一致**（首次启动/UI恢复/输入提示/失焦） | `game_surface.py --check --gate` |
| **G73** | **版本基准已冻结** | `game_net_mod.py --check --gate` |
| **G74** | 服务器权威经失败注入证明 | 越权/重复提交/乱序包须被拒绝 |
| **G75** | 模组 API 兼容矩阵按调用结果 | 不是「能加载」 |
| **G76** | **输入到光子延迟与相机可复现测量** | `game_latency_camera.py --check --gate` |
| **G77** | **音频信号链与渲染设置一致** | `game_audio_render.py --check --gate` |
| **G78** | 可玩性：关键路径可达且不软锁 | **Bot 通关 ≠ 体验一致** |
| **G79** | 测量方法可复现 | 不能凭感觉调 |
| **G80** | **导入设置与配置边界已固化** | `game_editor_data.py --check --gate` |
| **G81** | **输入设备与存档迁移链一致** | `game_input_ops.py --check --gate` |
| **G82** | 存档支持跨版本升级（v1→v3） | 不能只测最新版读最新版 |
| **G83** | **动画参数链一致**（含末端误差） | `game_animation.py --check --gate` |
| **G84** | **UI 布局链与物理参数链一致** | `game_ui_physics.py --check --gate` |
| **G85** | 碰撞层矩阵逐层核对 | **漏一层就穿墙** |
| **G86** | **流式七态与并发顺序契约一致** | `game_stream_thread.py --check --gate` |
| **G87** | **热更新八态与内存/默认值一致** | `game_hotfix_mem.py --check --gate` |
| **G88** | 引擎隐藏默认值逐项核对 | **未设置键就是潜在差异** |
| **G89** | **战斗手感参数链一致** | `game_feel.py --check --gate` |
| **G90** | **锁定/取消/会话/粒子/超分一致** | `game_session.py --check --gate` |
| **G91** | 手感在 30/60/120Hz 均实测 | **只测一个帧率不算通过** |
| **G92** | 超分/帧生成输入时间线已实测 | 记原始输入时间 |
| **G93** | **时间域与暂停掩码一致** | `game_time_pacing.py --check --gate` |
| **G94** | **关卡/教程/难度关系网一致** | `game_level_balance.py --check --gate` |
| **G95** | 隐形墙已入 schema | **移除不是改善是偏离** |
| **G96** | DDA 已记 effect_size 与 player_perceivable | 原作无则不得加入 |
| **G97** | **世界模拟与身体状态一致** | `game_world_body.py --check --gate` |
| **G98** | **社交/元游戏/反馈仲裁/浮点一致** | `game_social_meta.py --check --gate` |
| **G99** | 同帧仲裁顺序按原作实际记录 | **不能用模板默认值替原作决定** |
| **G100** | 浮点矩阵已固定 | **不能只靠固定种子** |
| **G101** | **音频传播与可读性一致** | `game_audio_read.py --check --gate` |
| **G102** | **感知性能与环境交互一致** | `game_perf_interact.py --check --gate` |
| **G103** | 混响区过渡曲线已采样 | **声音版隐形墙** |
| **G104** | 感知性能已跨硬件实测 | 建三级标准，不搬阈值 |
| **G105** | **任务状态与目标级语义一致** | `game_quest_dialog.py --check --gate` |
| **G106** | **库存手感与流程事务一致** | `game_inventory_flow.py --check --gate` |
| **G107** | 奖励双阶段事务与幂等键已实现 | 中断重开不得漏/重复奖励 |
| **G108** | 交易/拾取/奖励均有幂等键 | **重入是隐蔽 BUG** |
| **G109** | **战斗规则与 BOSS 契约一致** | `game_combat_rules.py --check --gate` |
| **G110** | **解锁节奏与输入反馈一致** | `game_progress_input.py --check --gate-check` |
| **G111** | 规则合约已绑定帧级证据 | **绑定可观测结果，不是函数名** |
| **G112** | 可达性按**动作闭包**验证 | 非物理可通行 |
| **G113** | **UI音效与过渡体验一致** | `game_ui_sound_transition.py --check --gate-check` |
| **G114** | **UI动态与镜头叙事一致** | `game_ui_motion_camera.py --check --gate-check` |
| **G115** | 同帧多音仲裁与音画样本偏移已记录 | **不能随机；不能只说同步** |
| **G116** | 交互失败已显式声明**故意沉默** | 否则会被当未实现补提示 |
| **G117** | **时间推进与动态经济一致** | `game_world_economy.py --check --gate-check` |
| **G118** | **声望派系与存档兼容一致** | `game_faction_save.py --check --gate-check` |
| **G119** | 时间推进已保存 advance_log | 只写"前进6小时"无法判定顺序 |
| **G120** | 存档迁移已跑**双向**且建 fixture 库 | 禁止默认零值补齐；损坏不清空 |
| **G121** | **联机延迟与 AI 可读性一致** | `game_netfeel_ai_trust.py --check --gate-check` |
| **G122** | **节奏与信息架构一致** | `game_pacing_info.py --check --gate-check` |
| **G123** | 命中回溯按**武器/动作**记录 | 全局统一化即偏离 |
| **G124** | 节奏同时输出**逻辑时间**与**真实时间** | 只按逻辑时间低估死亡重试疲劳 |
| **G125** | **运气体验与技巧天花板一致** | `game_luck_skill.py --check --gate-check` |
| **G126** | **重复可玩与玩家表达一致** | `game_replay_identity.py --check --gate-check` |
| **G127** | 保底六语义与险胜归属已记录 | 演出后才回滚保底损害可信度 |
| **G128** | 技巧已声明**帧基准**并跑完四档扫描 | 图形 trace 不能替代逻辑重放 |
| **G129** | **降级语义与失败体验一致** | `game_degrade_fail.py --check --gate-check` |
| **G130** | **内容冗余与系统极限一致** | `game_content_limit.py --check --gate-check` |
| **G131** | 降级已记**先砍什么×玩法权重**与升级滞回 | 画质档不是视觉配置，是规则档 |
| **G132** | 极限按**四维组合**测试 | 只测单值上限会漏掉组合边界 |
| **G133** | **证据链与仲裁层一致** | `game_evidence_chain.py --check --gate-check` |
| **G134** | **演进治理与老玩家迁移一致** | `game_migration_govern.py --check --gate-check` |
| **G135** | 每条目有**证据等级 1-8**；7 级以上不作验收 | 没有证据等级就只剩观点投票 |
| **G136** | 每个偏离与测试有 **owner 和到期日** | 否则长期分支在无人认领中腐烂 |
| **G137** | **可交付性与可观测性一致** | `game_delivery_observe.py --check --gate-check` |
| **G138** | **组织协作与发布终止一致** | `game_team_release.py --check --gate-check` |
| **G139** | 发布就绪**六项硬门槛**全为真 | 否则只能 `not-shippable` |
| **G140** | 门禁已被**故意改坏**验证过 | 82 个门禁可能全是假的 |
| **G141** | **物理—模拟耦合与资源生命周期一致** | `game_sim_resource.py --check --gate-check` |
| **G142** | **帧时序契约与脚本边界一致** | `game_frame_script.py --check --gate-check` |
| **G143** | 每个模拟值有**唯一 writer**，reader 声明读哪一帧 | 漂移的是所有权不是质量 |
| **G144** | **延迟一帧**已说明是设计/优化/偏离 | 不能把现象命名为原因 |
| **G145** | **力反馈与数值多值契约一致** | `game_haptics_numbers.py --check --gate-check` |
| **G146** | **输入硬件/AI听觉/云存档一致** | `game_input_percept.py --check --gate-check` |
| **G147** | 震感记录为**输出波形**，非「强度×时长」 | 两马达压一条曲线会让材质消失 |
| **G148** | **AI 听觉已先证存在** | 原版没有就不许以「更真实」为由补入 |
| **G149** | **程序生成边界与阴影契约一致** | `game_gen_shadow.py --check --gate-check` |
| **G150** | **交互反作用力与资源冷却语义一致** | `game_force_resource.py --check --gate-check` |
| **G151** | **同 seed 同世界**已在六层/输入闭包/精度/失败策略全一致下验证 | 「随机种子即确定性」是冲突 |
| **G152** | 阴影已先判 **shadow_authority**，低配未无差别关闭 | 阴影是玩法事实源时关阴影 = 关机制 |
| **G153** | **权威信任边界与玩家可调契约一致** | `game_trust_settings.py --check --gate-check` |
| **G154** | **帧预算分配与输入意图层一致** | `game_budget_intent.py --check --gate-check` |
| **G155** | 视觉命中与权威命中已拆成**四事件** | 视觉先显示命中不构成权威事实 |
| **G156** | `reset to default` **五义已分开** | 只写 `default: true` 无法解释恢复哪一个默认 |
| **G157** | **持久痕迹与通知聚合语义一致** | `game_trace_notify.py --check --gate-check` |
| **G158** | **加载连续性与非文本本地化一致** | `game_load_locale.py --check --gate-check` |
| **G159** | 痕迹预算按**玩法参数**记录，未静默删最旧痕迹 | 事实不能随渲染代理消失 |
| **G160** | 通知合并键与顺序**已抓帧判定** | 不能默认统一成单一 toast |
| **G161** | **地形植被与水体水下一致** | `game_terrain_water.py --check --gate-check` |
| **G162** | **布料毛发与反射透明一致** | `game_cloth_reflect.py --check --gate-check` |
| **G163** | 植被按**稳定性**验收（非数量） | 改密度即移动潜行边界 |
| **G164** | **镜中可见对象已按原版记录** | 反射是可见性语义 |
| **G165** | 预热状态已进入**加载事务** | 「异步编译可接受」是冲突 |
| **G166** | **环境耦合与系统健康一致** | `game_coupling_health.py --check --gate-check` |
| **G167** | **统计竞速与教学无障碍模组一致** | `game_stats_runs.py --check --gate-check` |
| **G168** | **风场相位同源**已记录 | 不同时间积分会累积相位漂移 |
| **G169** | 健康阈值来源**非 engine default** 且降级未改判定 | 单位是玩家是否仍获原版承诺 |
| **G170** | 调试读数**已绑相位** | 晚一帧是伪 bug 来源 |
| **G171** | speedrun **分记 RTA/IGT**，PB 保留历史 | 不能用一个「游戏时间」替代 |
| **G172** | **角色存在感与交互空间仲裁一致** | `game_presence_interact.py --check --gate-check` |
| **G173** | **可达性材质与音效覆盖一致** | `game_reach_material.py --check --gate-check` |
| **G174** | 可达性由**原版轨迹裁决** | 新 A* 可能双向判错 |
| **G175** | 材质转换**无未审 semantic_mismatch** | 中间表示不是转换器 |
| **G176** | **音效事件全部有声**，无静默回退 | 事件存在但声音不存在 |
| **G177** | **失败重试循环与刷新节奏一致** | `game_retry_spawn.py --check --gate-check` |
| **G178** | **容错撤销与物件复位一致** | `game_fault_reset.py --check --gate-check` |
| **G179** | **控制归还时间已单独记录** | 缺失会吞掉连按「再来一次」 |
| **G180** | 重试起手是**显式差异表** | 原版四档不是「满血/死亡」 |
| **G181** | 物件**四层状态**分别记 save_scope | 只记对象状态＝三重矛盾 |
| **G182** | **撤销窗口显式记录**（含为 0） | 撤销是延迟提交，不是软删除 |
| **G183** | **世界一致性与事件序一致** | `game_continuity.py --check --gate-check --allow-unknown` |
| **G184** | 无证据项**已标 unknown**，未填假数据 | 交付可验证缺口，不虚构 |
| **G185** | **四类冒充已排除** | 引擎默认值不能反推原版表现 |
| **G186** | **共置归属与天气空间语义一致** | `game_colo_weather.py --check --gate-check` |
| **G187** | 分屏**视口拓扑是状态机**，FOV 锁定轴已记 | 垂直分屏改变水平 FOV |
| **G188** | **档案七类隔离域**已拆分，删除六问已答 | 成就按账号、统计按档案 |
| **G189** | **天气两层状态机 + 玩法耦合契约** | 只复刻特效是偏离 |
| **G190** | **小地图两套坐标与更新相位**已记 | 失败在采样与排序 |
| **G191** | **交互事务契约一致** | `game_txn_contract.py --check --gate-check` |
| **G192** | **放置五种否决分别记录** | 吸附只是提示，合法性才是门槛 |
| **G193** | **交易预览是可审计净变化** | 确认与锁定是两个状态 |
| **G194** | **目标三视图与未探索区可见性** | 多目标需稳定排序键 |
| **G195** | **回放录制语义已声明** | 未声明不得称「完全忠实」 |
| **G196** | **满级后与洗点残留已建模** | 只写上限会漏经验溢出 |
| **G197** | **认知层与联机社交契约一致** | `game_cognitive_social.py --check --gate-check` |
| **G198** | **认知四层分流已判定** | 对齐真相还是对齐体验要有判据 |
| **G199** | **共识误解是一等资产** | 催生玩家行为＝玩法事实 |
| **G200** | **联机契约已定义** | 「能联机」≠「契约完整」 |
| **G201** | **非死亡失败 8 类 + 确认时刻 5 帧** | 分离是最核心 must-match |
| **G202** | **「没有预警」已显式声明** | 没有预警本身是设计 |
| **G203** | **世界纵深与难度维度一致** | `game_depth_difficulty.py --check --gate-check` |
| **G204** | **先证明原版行为再决定实现** | A–H 是观察维度不是新功能域 |
| **G205** | **先例建模为访问计数** | 「第一次」不是布尔 |
| **G206** | **难度八维系数已列出** | 不能只保留血量与伤害 |
| **G207** | **等待已判定为玩法还是加载** | 快速黑屏是偏离非现代化 |
| **G208** | **具身表现与输入契约一致** | `game_embodied_input.py --check --gate-check` |
| **G209** | **状态外显按通道记录 + 叠加矩阵** | 「中毒变绿」会漏掉一切 |
| **G210** | **物品四段生命周期已记录** | 只留数据库＝失去存在感 |
| **G211** | **沉默进选项表 + 半径未统一** | 统一半径＝抹平社交距离 |
| **G212** | **肌肉记忆六时序表已证明** | 只有日志和回放能证明 |
| **G213** | **声像冲突仲裁已定义** | 视觉射线统一触发＝听觉失真 |
| **G214** | **微观节奏与边界退化一致** | `game_micro_rhythm.py --check --gate-check` |
| **G215** | **帧语义六问已记录** | 平均延迟相同 ≠ 相位相同 |
| **G216** | **微停顿已逐项记录** | 帧生成帧不得冒充原生帧 |
| **G217** | **注意力仲裁从原版反推** | 新增弹窗＝修改注意力合同 |
| **G218** | **人格语气为可观察维度** | 「You died」有五种语气 |
| **G219** | **NaN/±0/±∞ 已触发** | 不得一律 sanitize |
| **G220** | **可破坏世界与连锁反应一致** | `game_destruct_chain.py --check --gate-check` |
| **G221** | **破坏粒度未擅自提升** | Voronoi 会改碎块弹射与可站立面 |
| **G222** | **承重支撑拓扑已反向推导** | 整段崩塌 ≠ 级联坍塌 |
| **G223** | **元素更新顺序与帧率依赖已验证** | 不许「顺手修好」 |
| **G224** | **约束参数按求解语义匹配** | 同参数是不同物理 |
| **G225** | **RNG 快照三档与固化时机已记录** | 决定存档点设在哪 |
| **G226** | **生活玩法与人格化痕迹一致** | `game_lifeplay_trace.py --check --gate-check` |
| **G227** | **生活玩法未「保留名字重做手感」** | 钓鱼＝拉力曲线不是难度档 |
| **G228** | **首次启动状态轨迹已逐字段记录** | 教学改动不可逆 |
| **G229** | **人格化痕迹已跨版本迁移** | 只迁移进度＝重置老玩家 |
| **G230** | **过场控制权矩阵已建立** | 不可跳过需证明 |
| **G231** | **离线功能降级图已建立** | 强制在线＝冲突 |
| **G232** | **经济按流量验证** | 稀缺性不许「修复」 |
| **G233** | **输入载体与数字权利一致** | `game_carrier_rights.py --check --gate-check` |
| **G234** | **触控按 pointerId 生命周期管理** | 按数组索引＝高危 |
| **G235** | **创作入口去留已作产品决策** | 删编辑器需评审 |
| **G236** | **保底与权利账本原子写入服务端** | 不得只在客户端 |
| **G237** | **数据布局已登记** | 字典/集合≠等价实现 |
| **G238** | **SIMD 保留标量黄金路径** | -ffast-math 破坏信条 |
| **G239** | **调试设施六态已记录** | 单开关代替＝违规 |
| **G240** | **意义层一致（含可辨态）** | `game_meaning_layer.py --check --gate-check` |
| **G241** | **声音语义按「声音事实」记录** | 混淆矩阵相同 > 响度相同 |
| **G242** | **成长按五通道记录** | 沉默事实不许补成「更爽」 |
| **G243** | **聚焦生命周期已逐事件录制** | 「恢复后正常」不算 |
| **G244** | **节拍偏移基于 beat_clock** | 故意错位也是 must-match |
| **G245** | **刻意坏味清单已建立** | 统一风格＝偏离 |
| **G246** | **社群锚点与版本语义已记录** | 不可达不能静默删词 |
| **G247** | **认知与耐力一致（含玩家侧证据）** | `game_cognition.py --check --gate-check` |
| **G248** | **心智地图已建** | 客观可达性 ≠ 玩家认知 |
| **G249** | **决策承诺语义已记录** | 故意隐瞒＝must-match |
| **G250** | **SL 可行性已实测** | 云同步不得消除 SL |
| **G251** | **长时间漂移已按 5 阶段采样** | 更舒适 ≠ 更忠实 |
| **G252** | **上手曲线已记前 5/30/120 分钟** | 理解完成度才是目标 |
| **G253** | **确定性信任两本账已测** | 成功包络非平均成功率 |
| **G254** | **责任归因六方已记录** | 不得新增 MVP/死亡原因 |
| **G255** | **推断层一致（含玩家推断）** | `game_inference.py --check --gate-check` |
| **G256** | **常识测试矩阵已建立** | 反常识需运行时证据 |
| **G257** | **OOB 地图已记录** | OOB 不是物理 bug |
| **G258** | **通道权威性四级无越级** | 动画命中判定未中＝撒谎 |
| **G259** | **随机接口已复现** | 不只复现分布 |
| **G260** | **失败可解释性四级已记** | 不只死亡需要解释 |
| **G261** | **设备老化与校准已入 profile** | 老化设备体验不同 |
| **G262** | **关系状态一致（含跨会话证据）** | `game_relation.py --check --gate-check` |
| **G263** | **离开与弃坑状态已记录** | 统一成流失原因会丢信息 |
| **G264** | **第 N 次四层计数已建立** | 布尔 seen 会误判可跳过 |
| **G265** | **幽灵内容已按六级分级** | 门后无模型 ≠ 无承诺 |
| **G266** | **第一次三层已记录，未剧透** | 不可再生情绪只能记偏好 |
| **G267** | **私有公平规则与笨拙容忍已取证** | 笨拙不是残障 |
| **G268** | **共谋未被揭穿** | 显示「这只是演出」即破坏 |
| **G269** | **身份与时序一致** | `game_identity.py --check --gate-check` |
| **G270** | **实体身份四层已建立** | 去重＋数量＝消灭玩家的剑 |
| **G271** | **方言已录为动作短语** | 含无效输入副作用 |
| **G272** | **意外组合三级分类** | 社区契约不默认修复 |
| **G273** | **惯例层与原版层并存** | 反惯例不是落后 |
| **G274** | **挂机/失败美学/时间礼物已取证** | 稀缺性本身就是内容 |
| **G275** | **社会层与版本地层一致** | `game_social.py --check --gate-check` |
| **G276** | **知识声明三层已建立** | 老玩家教的是旧版本 |
| **G277** | **假可玩性已分级** | 界面先承诺、系统再拒绝 |
| **G278** | **第二职业可观测契约已记录** | 生态失效≠没有 Mod |
| **G279** | **版本地层 DAG 已建立** | 不得写「日版总是更难」 |
| **G280** | **仪式性浪费与苦行契约已取证** | 无成就≠不苦行 |
| **G281** | **个性化真伪与跨通道名称已验证** | 四个破绽接缝 |
| **G282** | **外围层与身体接口一致** | `game_periphery.py --check --gate-check` |
| **G283** | **元游戏外围层按状态机记录** | 客户端可缺是硬要求 |
| **G284** | **玩家自救已录为第二控制流** | 删掉共同语言才是偏离 |
| **G285** | **物理外设逐设备建档** | 禁止统一通用手柄映射 |
| **G286** | **游玩环境考古已逐环境取证** | 客厅≠书房 |
| **G287** | **元进度五层与账号连接流已记录** | 禁止自动合并 |
| **G288** | **玩家作品管线与通关后状态机已取证** | 照片模式不是截图键 |
| **G289** | **生活层与文化层一致** | `game_life.py --check --gate-check` |
| **G290** | **会话承诺曲线与退出尾部已取证** | 管理的是承诺不是时长 |
| **G291** | **受控随机七通道与透明度三层已锁定** | 无法归因才是问题 |
| **G292** | **文化层十维与区域变体已记录** | 不得统一成中立版 |
| **G293** | **排序稳定性与命名截断契约已验证** | 不能悄然取代未定义顺序 |
| **G294** | **展示层与删除权已取证** | 删除权不是删号 |
| **G295** | **精通曲线与技能遗忘已记录** | 没有回归训练就不得插入 |
| **G296** | **第三空间与弱互动已取证** | 优化掉等于删除社交 |
| **G297** | **元协议层一致** | `game_meta.py --check --gate-check` |
| **G298** | **沟通元语言按模态矩阵记录** | 统一 ping 会抹平语法差异 |
| **G299** | **行动前风险账本已逐字段可回放** | 决定选择属于谁 |
| **G300** | **行为记忆带证据、期限与退出路径** | 黑箱读心冲突 |
| **G301** | **外部工具契约已建立** | 字段语义比 JSON 美观重要 |
| **G302** | **价值信号全通道一致** | 稀有度不是颜色表 |
| **G303** | **认知地图锚点可复现** | 不是地图几何 |
| **G304** | **沉默区域未被贴心化** | 沉默是规则不是空缺 |
| **G305** | **变体与载体层一致** | `game_variant.py --check --gate-check` |
| **G306** | **非完整版本七态与继承状态图已取证** | 不得静默覆盖体验版档 |
| **G307** | **秘籍按五层输入窗口建模** | 秘籍是发布内容 |
| **G308** | **非玩家参与者信息层已分离** | 给观众看 ≠ 给玩家听 |
| **G309** | **实体载体与纸质地图已记录** | 数字替身不得覆盖原版 |
| **G310** | **错误美学与存档控制权已取证** | 不得替换原版错误弹窗 |
| **G311** | **边缘状态层一致** | `game_edge.py --check --gate-check` |
| **G312** | **试玩机十二类状态已取证** | 超时立刻黑屏；当 bug 修掉 |
| **G313** | **反盗版失败屏与故意异常已记录** | 不得悄悄修掉版本表面 |
| **G314** | **CD-KEY 字符消歧已按原版语义** | 不得自动纠错 |
| **G315** | **Credits 排序按原版事实记录** | 删除原作者是署名缺陷 |
| **G316** | **支持者名单未被善意破坏** | 不得删除/合并/改正拼写 |
| **G317** | **证据态四态已显式填写** | 不可验证要升级为字段值 |
| **G322** | **构建矩阵已展开** | 不能只有两栏 |
| **G323** | **baseline 同环境生成** | OS/headless 都会影响渲染 |
| **G324** | **基线不可知不伪造 PASS** | 只产出 observed_durations |
| **G325** | **音频指纹按时间窗切段** | LGPL 合规须审查 |
| **G326** | **物理表面用可复核尺度** | 不以艺术照为 must-match |
| **G327** | **人工批准记 reviewer 与 baseline hash** | trace 不得冒充 baseline |
| **G328** | **登记表已归一化** | 无重复 key / 全有 kind / 重名已标记 |
| **G329** | **能力矩阵已按 kind 解读** | 不得把缺失 N 当能力缺口 |
| **G330** | **空工作区不得读成全绿** | 通过 0 / 跳过 119 须显式警告 |
| **G331** | **四路验收端到端自测通过** | fixture→capture→join→durations→verify |
| **G332** | **缺工具的路记为 tool_missing** | 不得伪造为通过 |
| **G333** | **基线不可知只产出 observed_durations** | T_min/T_max 必须 null |
| **G334** | **SPDX Build 接入自测通过** | 合规片段过 / 缺关系被拦 |
| **G335** | **Build 必含三条强制关系** | 缺 hasInput/hasOutput/invokedBy 即阻断 |
| **G336** | **buildId 不得当全局唯一键** | 须自加 externalIdentifier 全局层 |
| **G337** | **默认不写 buildStartTime/EndTime** | 写了须显式标注 |
| **G338** | **真实捕获不得走 --verify** | 无真值时必须退出 1 |
| **G339** | **自测与真实取证分两条路径** | 不得以合成自测冒充取证 |
| **G340** | **真实导入须先 ffprobe 探测** | fps 不得猜 |
| **G341** | **门禁混沌测试能执行并产出报告** | 故意改坏是唯一有效验证 |
| **G342** | **空台账不得静默通过** | 0 条 ≠ 无阻断项 |
| **G343** | **枚举字段须白名单校验** | 黑名单式拦不住填错的值 |
| **G344** | **混沌覆盖率须被测量** | 拦截率必须伴随覆盖率 |
| **G345** | **配对字段须差异化毒化** | 填相同值可能掩盖对比失效 |
| **G346** | **须做语义毒化** | 合法值+内部矛盾是最危险形态 |
| **G347** | **跨字段一致性须被检查** | 字段合法 ≠ 行自洽 |
| **G348** | **A 类盲区须归零** | 已修，不得回退 |
| **G349** | **毒化器须适配 md 台账** | 当 CSV 处理会摧毁结构 |
| **G350** | **接口不匹配须与崩溃分开** | 测试器没适配 ≠ 门禁失效 |
| **G351** | **101 项须全部参与统计** | 接口不匹配要适配，不能留作『没测』 |
| **G352** | **汇总须显示门禁自身可信度** | 必须同时报拦截率与覆盖率 |
| **G353** | **skip 须细分到物种** | 不能混成『没测上』 |
| **G354** | **缺数据不得等同在容差内** | 与【空表即通过】同源 |
| **G355** | **枚举须标 constraint / must_match** | 无 evidence 列不做证据断言 |
| **G356** | **基线必须 rc=0，毒化才有效** | 否则『测了个寂寞』 |
| **G357** | **适配器须完全接管通用路径** | 通用 init 先失败会判 skip |
| **G358** | **值毒化无效时用结构毒化** | 存在性检查：删键才有效 |
| **G359** | **拦截率须区分强/弱证据** | 基线 rc≠0 时只证明『它会拦』 |
| **G360** | **强证据率靠逐脚本 fixture** | 通用填充猜不出专属枚举 |
| **G361** | **强证据率须标注目录完备型上限** | 「没记全」正是这类要拦的，不该凑 |
| **G362** | **填充/毒化须按台账实际格式分派** | YAML/MD 按 CSV 处理会**写坏文件** |
| **G363** | **目录完备型须用增量完备性验证** | 补齐一项 → 缺失计数应降一项 |
| **G364** | **缺失计数行未打印 ≠ 仍有缺失** | 抓不到数字要判 0，不是沿用上次值 |
| **G365** | **不得用「删行 → blocker 上升」做通用完备性** | 逐行校验型删行＝删掉 blocker 源，必然误判 |
| **G366** | **目录扫描器只能列候选，不得下判定** | 是否目录完备型须人工登记 |
| **G367** | **两个格式嗅探器结论必须一致** | 不一致 → md 被当 CSV 写回 |
| **G368** | **毒化后文件必须仍可解析** | Traceback = 毒化器写坏文件，结论不可信 |
| **G369** | **不得用填充冒充毒化** | 填充填合法值，测不出内容敏感性 |
| **G370** | **探针覆盖度 ≠ 全体覆盖度** | 未参与的就是未测，须显式报出 |
| **G371** | **合成改瘦后必须重测基线** | 合成破坏合法性 → 不得判 A（假阳性） |
| **G372** | **关键词只能提示，不得判定 A/B** | 措辞不统一：双向都有反例 |
| **G376** | **混沌器能力自检须通过** | 语义毒化 / fixture / pair / ENUMS 读取四能力 |
| **G377** | **说明性门禁不得伪装成软警告** | 🔴 字符串 tpl 会被逐字符拆成命令，hard 会静默失效 |
| **G378** | **抽样混沌不写全量缓存** | 抽样的 N/N 会谎报全量拦截率 |
| **G379** | **全量混沌必须写入 full 口径缓存** | 否则汇总显示"从未执行全量" |
| **G380** | **抽样不得污染全量口径** | 抽样只写 `sampled`，不得碰 `full` |
| **G381** | **完成声明须回读代码核实** | 🔑 声明清单 + `--scan-doc`，未兑现即不得声称完成 |
| **G382** | **文档里的门禁编号不得凭空出现** | `--scan-doc`：文档有、代码无 |
| **G383** | **全部声明清单须持续兑现** | `--verify-all`：旧轮回退也要被抓 |
| **G384** | **最新一轮须已写声明清单** | `--check-current`：守「有没有写」而非「写了是否兑现」 |
| **G385** | **`--all` 三段须全部执行**（独立校验） | `--audit-all`：🔑 **不看 rc**，查段体内容特征 |
| **G386** | **`ALL_CONTENT` 特征串须段体独有** | `--check-content`：🔑 守**判据本身**，防漂移/非独有 |
| **G387** | **门禁命令不得误重复** | `--check-dup-cmds`：🔑 刻意重复须**逐个 ID** 登记 |
| — | **登记集合须等于实际重复组** | 第八十二轮：不等即**阻断**（非警告） |
| — | **守卫须声明『守的是什么』** | 第八十三轮：`guards` 语义字段，防「存在但无关」 |
| — | **守卫须『自报能力』** | 第八十四轮：`--declare-capability`，子串匹配→**双向声明** |
| — | **能力须『可探测』** | 第八十五轮：`--probe-capability` 真跑一次 + `PROBE_OK` |
| — | **须有『外部可观测副作用』** | 第八十六轮：`CAPABILITY_EFFECT`，打印标记无法蒙混 |
| — | **须产出『文件系统产物』** | 第八十七轮：`CAPABILITY_ARTIFACT` + 纳秒戳，**快照须运行前取** |
| — | **产物内容须与『独立真值』一致** | 第八十八轮：`CAPABILITY_ARTIFACT_KEYS`，防写对形状写错值 |
| — | **真值须『跨进程』取得** | 第八十九轮：rc 合法域仅 {0,1}；🔴 只隔离入口路径，**不隔离代码** |
| — | **能力框架须『覆盖多个能力』** | 第九十轮：推广到 `chaos-full`；🔑 新增**下界断言**（量级而非形状） |
| — | **下界须有『外部锚点』** | 第九十一轮：`CAPABILITY_ANCHOR_FLOOR`；🔴 基线被污染时锚点须能兜住 |
| — | **锚点分母须用『台账型脚本数』** | 第九十二轮：`_anchor_denom()`；🔑 两类锚点 `floor`/`domain` |
| — | **同一判据不得写两处** | 第九十三轮：`GATE_RC_DOMAIN` + G388；🔴 只数出现次数**防不住改值** |
| — | **字面量相同 ≠ 语义相同** | 第九十四轮：`RC_DOMAIN_ALLOW`；🔴 astgrep 的 `1`=有命中，**不是**阻断 |
| — | **豁免须外部化且『读不到』≠『没有』** | 第九十五轮：`ledger/rc_domain_allowlist.txt`；🔴 豁免文件本身无人守 |
| — | **豁免须带批准轮次、不得僵尸** | 第九十六轮：G389；🔴 中文数字正则缺 `十`/`百` 会把轮次算错一个数量级 |
| — | **推送须先验漏传、后验完整性** | 第九十七轮：G390 `--check-leak`；🔴 `push_api` 已改用 argparse（手写参数无法被静态验证） |
| — | **『已推送』须回读远端逐条 sha 才成立** | 第九十八轮：G391；🔴 **门禁自身写缓存** → 该门禁不能放进自动门禁 |
| — | **文件权限须取自 git 索引，不得看磁盘 x 位** | 第九十九轮：🔴 沙盒 `os.access(X_OK)` 恒 True → 266 文件曾被全推成 100755；`core.fileMode=false` 时 chmod 无效，须 `update-index` |
| — | **symlink 的 blob 内容是链接路径，不是目标文件内容** | 第一百轮：G393；🔴 自测曾复刻被测逻辑 → 回退实现也测不出（自证循环），须抽唯一实现 |
| — | **gitlink 无磁盘文件，不得用 `os.path.exists` 全量前置检查** | 第一百零一轮：G395；🔴 有 submodule 时旧流程会让**整个仓库**推不出去（非仅漏传） |
| — | **审计型入口不得自行阻断，须另设断言门禁** | 第一百零二轮：G396；🔴 让审计自己阻断会诱导它返回 0 → 问题被掩盖 |
| — | **遗留台账须记路径指纹，不能只记数量** | 第一百零三轮：G397；🔴 "数量对但文件变了" 只靠 count 完全测不出 |
| — | **网络异常不得抛出，须返回可判读的错误** | 第一百零四轮；🔴 崩溃与"拒绝给结论"都是 rc=1，调用方分不清 |
| — | **完整清单须落盘，不得只留在远端** | 第一百零四轮：G398；🔴 远端不可达时"哪些文件不对"就答不了 |
| — | **互补的两层须登记并存，且命令必须互异** | 第一百零五轮：G399；🔴 同一实现挂两个门禁号＝看起来两层，实际一层 |
| — | **造破坏样本后须先确认它真的被破坏了** | 第一百零五轮；🔴 破坏没做成时结果是 rc=0，会被误记为兼容通过 |
| G31 | **功能对照表无未定项、无危险组合** | `scripts/feature_matrix.py --check --gate` |
| G32 | **文本排版五维一致**，未定项已补证据 | `scripts/text_layout.py --gate` |
| G33 | **26 类故障六栏齐全**且原版行为已采集 | `scripts/fault_matrix.py --check --gate` |
| G34 | **安装契约 13 字段 + 残留 13 处均有 owner** | `scripts/install_contract.py --check --gate` |

**一次跑完**：`python3 scripts/run_all_gates.py --work work/ --strict`
（`--list` 只看清单不执行）。**⚠️ 自动门禁全绿 ≠ 可以收工** ——
人工门禁（golden 签字、故障注入、视觉审批、六个实测；
许可法务仅 `third_party` 时需要）
一项没签字就不得声明完成。

**门禁结果必须写进版本库**，不能只作为会话内的口头结论。

---

## 三·补：深挖时顺手解决的 BUG

**挖掘和修 BUG 不是两件事，是同一件事的两面。**
读到一段代码觉得"这里不对"——那往往正是最重要的发现（实证：拖拽 35 个机制里，
5 个直接产生 bug 的全是这样挖出来的）。

流程：**记录 → 判定 → 分诊**

```
挖到可疑处
   ├─ 记一张 B-N 卡（30 秒，先记录不要深究，别打断挖掘主线）
   ├─ 判定：真 BUG / 设计意图 / 待确认
   │         ⚠️ 判不出来 → 进 unknown，不许默认当成 BUG 修
   └─ 分诊：简单 → 当场给迁移后实现方案 + 立刻修 + 打勾
            复杂 → 记入清单 + 进批次，继续挖
```

**"简单"判据（五条全满足才当场修）**：

| # | 条件 |
|---|---|
| 1 | 改动局限在**单个函数/单个文件** |
| 2 | 不涉及并发模型、数据格式、跨端契约 |
| 3 | 有明确可执行的验证方式 |
| 4 | 不需要重构（不是"顺手重构"的借口） |
| 5 | 不会打断当前文件的深挖主线 |

**⚠️ 修原版 BUG = 有意行为变更**：原版的 BUG 也是用户当前体验的一部分，
修掉它必须登记为有意变更并走 golden 批准（见 `flow/S2B`）。
**不登记的 BUG 修复，等同于静默改需求。**

**"复杂"进批次**：跨多模块、涉锁/事务/编码/跨平台、需重构、或需 golden 批准的行为变更。

详见 `flow/S2B-BUG与审查发现.md`、`references/BUG判定与即时修复.md`。

---

## 四、明确拒绝的流行做法

| 流行做法 | 为什么不采纳 |
|---|---|
| **先做 MVP，再补功能** | MVP 会把未知项变成待办黑洞，未知功能永远回不来 |
| **先清理再迁移** | 没有 characterization 与契约的清理会让历史行为静默消失 |
| **够用就行 / 结果合理即可** | 复刻的标准是原版实际行为，不是模型认为合理的样子 |
| **产出衰减就收工 / 让模型决定何时完成** | 收工由覆盖率、unknown、契约、golden、故障注入与人工批准共同决定 |
| **测试通过即等价** | 测试可能漏路径、golden 可能被手改、断言可能被削弱 |
| **自动 approve 视觉 baseline** | 把目标从"复刻旧体验"改成"让新实现通过"；AI 不得代签 |
| **单一全局阈值衡量所有 UI** | 图标和动画不能同一个标准；阈值必须按状态分层 |
| **端到端通过即迁移完成** | 端到端不覆盖视觉状态、边界、时序、可访问性 |
| **多 agent 天然更准** | 无边界并行只会复制错误；只有隔离 + 结构化产物 + 独立审查才提高可靠性 |
| **纯坐标点击做原版自动化** | 与"体验细节不能丢"直接冲突；WPF 视觉树多装饰层，坐标必然脆弱 |
| **录屏回放当回归证据** | 无法区分环境噪声与真实差异，只能作人工证据 |
| **图形相似搜索替代控件树 diff** | 容易掩盖控件缺失（长得像 ≠ 功能在） |
| **反编译结果当源码原文** | 是 B 级证据（反编译产物），不许当原文用；且须先确认授权 |
| **无证据就标"已解决"** | 拿不到原版运行时/源码/授权说明时，缺口应写进报告，不许用新版默认实践补 |
| **用侵权字体替代原版字体** | 允许用更优设计替代实现，**不允许用侵权替代资源** |
| **"像素不一致就失败"** | 太粗：会误杀亚像素文本、动画相位、合法字体替换 |
| **"像素一致就通过"** | 也不足：整棵结构已变只是凑巧相似时会放过 |
| 抗锯齿免疫/区域忽略当豁免 | 只能降噪声；真实圆角/渐变/阴影变化仍须进契约回归 |
| **tree-sitter diff 当 UI 等价性** | 文本结构相似 ≠ 运行时控件树等价 |
| 只保留分类名删掉原始截图 | 后续无法复核；只留截图不解释也无法完成穷尽验收 |
| **"功能等价即完成"** | 与"体验细节不能丢"冲突；结构等价但手感不同应判**部分完成** |
| 全局代码风格规则 | 易把 `var` 转 `const`、改 Win32 时序，**却遗漏原版边界** |
| **Healenium 自愈定位器** | 目标函数是"测试通过率"；会绕过定位稳定要求，S2 成果被悄悄作废 |
| **iFixFlakies 自动修测试** | 修的是 oracle 而非实现，S3 性质判定失去依据 |
| NonDex / DeFlaker | 让测试更宽松 / 跳过可疑测试，与"不轻易收工"相悖 |
| 异常指纹合并 | S2B BUG 清单少报，S6 执行质量被系统性低估 |
| 用已归档工具进门禁（fMBT / NModel） | 不与理念冲突，但**与"门禁必须可持续"冲突** |
| **用 Chromium 截图等同文本等价** | 不暴露 glyph/cluster/Bidi 根因；截图只作最终检查 |
| **"没有崩溃"等同韧性等价** | 两条不同的门禁；原版行为未采集就不许谈新版韧性 |
| **"概率通过"的随机并发测试** | 与穷尽理念冲突；只能作发现器，须配确定性 seed |
| **"能粘贴文本"等同剪贴板复刻** | 忽略 CF_HTML、CF_DIB、OLE、私有格式 |
| 只测 happy path | 原版异常行为未采集，26 类故障目录要逐类过 |
| Sentry 作唯一崩溃源 | 聚合优先，不保证单用户证据完整；只作上报渠道 |
| 自动 font subset / unicode-range | 漏字符、漏 OT feature；`unicode-range` 只控下载不删字形 |
| **"能跑就行"/"手感差不多"** | 略过确定性、desync、coyote time、死区验证 |
| "帧率更高说明更好" | 可能破坏固定时间步与回放 |
| "不同 GPU 结果不同很正常" | 掩盖真实实现差异，需软渲染基线证伪 |
| "有 savestate 就能回滚" | SaveState 只记物理位置速度，副作用须自行恢复 |
| "提取脚本一键搞定" | 版本/加密/平台都可能失效；权利须人工确认 |
| "原版资源可自由再分发" | 权利状态须进 SBOM，**不能由工具自动判定** |
| 把 modern-replacement 伪装成 exact-match | 与理念最危险的冲突 |

> **本 Skill 的目标函数是"差异暴露率"** ——
> 要求测试在发现差异时**失败**，且失败可归因到具体字段、状态、像素。
> **任何外部工具进门禁前，必须重写其目标函数以匹配"差异暴露率"。**

---

## 五、目录与产物

```
README.md             导航：三分钟看清全局（新人先读这个）
SKILL.md              总控入口（本文件）
flow/                 十四张阶段卡（S0/S1-S7 + S2M/S2A/S2P/S2T/S2B/S7V）+ 门禁说明
method/               方法论沉淀（穷尽挖掘 / 整合设计 / 审查 / 执行 / 跨端）
references/           按需加载的清单（九切角、体验参数挖掘、资产与资源总清单、
                                     体验维度总清单、原版取证、许可与依赖治理、
                                     **树对齐与差异归因**、**不变量与差分测试**、
                                     **语义证据层**（文本/并发/故障/安装）、
                                     **编排与证据治理**（能力契约/签字链）、
                                     **游戏引擎复刻**（确定性/GPU/匹配反编译/资产）、
                                     **运行时取证与二进制覆盖**（插桩/二进制diff/AI边界）、
                                     **原版机制与目标栈**（WPF机制/Tauri+React+Rust/数值保真）、
                                     **状态迁移与可观测性**（迁移保真/IPC/日志遥测）、
                                     **安全信任与无障碍**（签名/更新/WebView/离线/IME）、
                                     **跨引擎迁移与二次创作**（**主场景**：坐标/概念映射/资产/物理/动画/四分类）、
                                     证据等级、性质判定、BUG 判定、机器查询规则、
                                     视觉稳定化、工具白名单、负面教训库）
assets/               模板（证据卡、能力包、功能卡、BUG 卡、视觉状态契约、
                                 体验参数清单、资产与资源清单、文案与全维度清单、
                                 **功能对照表**、等价性三层、清单、契约）
scripts/              可执行检查（**run_all_gates.py 一键全跑**、台账、括号、
                                 编号、注释提取、字段差集、体验参数提取与差集、
                                 资产清点、文案提取与差集、原版采集、许可门禁、
                                 动效门禁、**树对齐**、**差异归因**、**环境锁**、
                                 **差分测试与 oracle 分类**、**文本排版金标准**、
                                 **故障行为矩阵**、**安装契约**、覆盖率门禁、
                                 BUG 分诊门禁、AST 查询、解析状态、
                                 视觉状态校验、跨端）
scripts/tools/        **外部工具封装层**：registry.yaml（66 个工具）+ tool_run.py
                                 调度器 + 9 个具体封装；身份校验、诚实降级
scripts/game_*.py     **游戏引擎复刻**：game_replay（确定性/desync）、
                                 game_render（渲染取证/基线）、game_asset（资产取证）
scripts/runtime_forensics.py / binary_coverage.py / context_pack.py
                      **运行时取证、二进制覆盖、AI 参与门禁**（第八轮）
scripts/wpf_mechanism.py / target_stack.py / value_fidelity.py
                      **原版机制清单、目标栈能力矩阵、数值保真**（第九轮）
scripts/state_migration.py / ipc_surface.py / log_parity.py
                      **状态迁移保真、IPC 面、日志遥测对等**（第十轮）
scripts/security_trust.py / network_resilience.py / a11y_ime.py
                      **安全信任、网络韧性、无障碍与 IME**（第十一轮）
scripts/engine_migration.py                **跨引擎迁移与二次创作**（主场景）
scripts/game_content.py / game_combat_ai.py / game_rng_save.py
                      **游戏深化**：内容穷尽 / 战斗手感与 AI / 数值与存档
scripts/game_surface.py / game_net_mod.py
                      **功能表面**（首次启动/输入提示/失焦/UI恢复）/
                      **版本基准·网络·模组**
scripts/game_latency_camera.py / game_audio_render.py
                      **输入到光子延迟与相机** / **音频信号链·渲染·可玩性**
scripts/game_editor_data.py / game_input_ops.py
                      **编辑器·构建·配置与 buff** / **输入设备深化·存档链**
scripts/game_animation.py / game_ui_physics.py
                      **动画参数链**（根运动/重定向/末端误差）/
                      **UI 布局链与物理参数链**
scripts/game_stream_thread.py / game_hotfix_mem.py
                      **流式七态与大世界精度** / **并发顺序契约** /
                      **热更新八态·内存·引擎隐藏默认值**
scripts/game_feel.py / game_session.py
                      **战斗 juice（顿帧/霸体/伤害数字）** /
                      **移动手感（coyote/跳跃缓冲/可变跳跃）** /
                      **锁定取消·会话·粒子·超分 HDR**
scripts/game_time_pacing.py / game_level_balance.py
                      **时间域与暂停掩码** / **帧 pacing 与 AA** /
                      **关卡空间体验·教程·难度关系网·DDA**
scripts/game_world_body.py / game_social_meta.py
                      **世界模拟与离线六层** / **身体状态与负重舍入** /
                      **社交表达·元游戏·反馈仲裁·浮点根因**
scripts/game_audio_read.py / game_perf_interact.py
                      **声音传播七通道·混响区过渡** / **视觉可读性采样** /
                      **感知性能·环境交互·拍摄回放**
scripts/game_quest_dialog.py / game_inventory_flow.py
                      **任务状态图·对话六级跳过** / **背包手感·稳定排序** /
                      **配点撤销·地图导航·死亡重生事务**
scripts/game_combat_rules.py / game_progress_input.py
                      **伤害五管线·DOT快照·免疫语义** / **BOSS阶段·招式分布** /
                      **能力门闭包·输入反馈·动作仲裁**
scripts/game_ui_sound_transition.py / game_ui_motion_camera.py
                      **UI音效同帧仲裁·过渡十阶段** / **数字滚动中断·假进度** /
                      **镜头控制权五元组·故意沉默**
scripts/game_world_economy.py / game_faction_save.py
                      **时间分钟基·跳跃批量模拟·价格冲击链** /
                      **声望看见vs生效·存档双向迁移·光照玩法**
scripts/game_netfeel_ai_trust.py / game_pacing_info.py
                      **回溯按武器·外推制造滑步·延迟不对称** /
                      **AI意图可读·公平感来自信息对称** ·
                      **节奏时间函数·信息架构·存档心理契约**
scripts/game_luck_skill.py / game_replay_identity.py
                      **保底六语义·险胜归属·技巧帧基准·144Hz扫描** /
                      **随机十层·NG+契约·外观槽位·展示场景·故意不告知**
scripts/game_degrade_fail.py / game_content_limit.py
                      **降级先砍什么×玩法权重·升级滞回·卡住系统检测** /
                      **删除结果才是冗余判定·极限四维组合·诚实进度·同义证明**
scripts/game_evidence_chain.py / game_migration_govern.py
                      **四层证据链·取证顺序·证据八级·偏离许可** /
                      **三棵树·原版更新窗口·迁移四表·双轨三态·自动化四档**
scripts/game_delivery_observe.py / game_team_release.py
                      **交付清单·首启契约·卸载七类·最后一帧环形缓冲** /
                      **发布就绪六门槛·双向交接·预算闸门·混沌验证门禁**
scripts/game_sim_resource.py / game_frame_script.py
                      **驱动权·布娃娃/IK/根运动·碰撞音事件源·资源身份六种** /
                      **一帧九阶段·延迟一帧五方向·异常四态·场景加载事务**
scripts/game_haptics_numbers.py / game_input_percept.py
                      **触觉波形·左右马达频段·自适应扳机状态机·数值五值** /
                      **输入硬件物理层·AI听觉先证存在·云存档租约·Pass顺序·模态栈**
scripts/game_gen_shadow.py / game_force_resource.py
                      **生成六层·输入闭包·失败四结果·shadow_authority·接触阴影** /
                      **推不动五态·冷却起点八候选·差一点舍入·过场阶段书签·软性禁止**
scripts/game_trust_settings.py / game_budget_intent.py
                      **client_claim_facts·命中四事件·三套信任拓扑·五义默认值** /
                      **帧预算借还·意图层稳定排序键·生命周期九态·死亡四类**
scripts/game_trace_notify.py / game_load_locale.py
                      **持久痕迹八组·痕迹预算是玩法参数·读档四答案·四把合并键** /
                      **加载八域连续性·口型/配音/文化/BiDi·相机四个面·成就四层**
scripts/game_terrain_water.py / game_cloth_reflect.py
                      **地形采样精度·LOD呼吸·植被三实例与稳定性·水下状态机** /
                      **布料三拓扑·反射可见性·透明六字段·陀螺仪·粒子确定性·响度四表·预热**
scripts/game_coupling_health.py / game_stats_runs.py
                      **风场相位同源·耦合矩阵·系统健康阈值来源·可观测性相位** /
                      **统计口径·回放四层·RTA/IGT·教学强制边界·无障碍替代三语义·模组顺序**
scripts/game_presence_interact.py / game_reach_material.py
                      **存在感与责任边界·能否挤过三态·同伴五事实·注视状态机·并发稳定排序键** /
                      **可达性两阶段·能力逐参数·BRDF样本·工作流损失·色调映射·音效覆盖审计**
scripts/game_retry_spawn.py / game_fault_reset.py
                      **失败十相位·控制归还≠设备重连·重试起手差异表·再来一次六字段·Boss重试** /
                      **刷新压力曲线·容错撤销窗口·确认焦点·存档覆盖·物件四层状态·复位双时钟**
scripts/game_continuity.py
                      **存档六类状态分类器·冻结点·读档六步·氛围层取证·同帧四种时钟·事件稳定排序键·意图边界矩阵·HUD双时钟**
scripts/game_colo_weather.py
                      **分屏六层同一帧·视口拓扑状态机·垂直分屏改FOV·每视口预算·监听拓扑·档案七类隔离域·天气两层状态机·玩法耦合契约·小地图两套坐标**
scripts/game_txn_contract.py
                      **交互事务六副作用·建造五种否决·交易可审计净变化·确认≠锁定·目标三视图·路径轮询·成长逐区间·洗点残留·满级溢出·回放事实vs像素**
scripts/game_cognitive_social.py
                      **认知四层分流·共识误解七字段·故意误导最小感知条件·高亮两条渲染路径·邀请9态·踢出/退出双状态机·中途加入8子集·掉落分配·队友伤害·非死亡失败8类·确认时刻5帧**
scripts/game_depth_difficulty.py
                      **世界历史三层证据·visibility_scope七级·先例是访问计数·知识四层边界·地标五显著性·区域六签名·难度八维·中途调整七语义·等待十二事实与原子性·涌现最小复现链**
scripts/game_embodied_input.py
                      **状态外显八字段·多状态叠加矩阵·物品四段生命周期·耐久视觉可读性·装备切换输入契约·非语言六字段·沉默是选项·肌肉记忆六时序表·声像仲裁·非死亡时间损失·多人距离带**
scripts/game_micro_rhythm.py
                      **帧语义六问·十二通道·微停顿逐项·接缝四输出·注意力仲裁七字段·强制注意力返还合同·人格语气可观察维度·沉默基线·NaN/±0/±∞/溢出必触发·退化世界**
scripts/game_destruct_chain.py
                      **破坏粒度四种·承重支撑图·碎块四计时器·NavMesh只能阻塞不能创造·开洞音频衍射·元素不对称矩阵·更新顺序决定确定性·风场耦合·灭火残留·绳索约束语义·荡绳可达连续函数·RNG快照三档与固化时机·氛围密度**
scripts/game_lifeplay_trace.py
                      **钓鱼拉力—时间曲线·上下马控制权交接·宠物关系反馈层·住宅因果图·首次启动状态轨迹·失败教学三时序·人格化痕迹五类·设置迁移语义·过场控制权矩阵·选择延迟层级·不可跳过需证明·离线功能降级图·经济四表五场景·稀缺性**
scripts/game_carrier_rights.py
                      **触控pointerId生命周期·手势状态机竞争·虚拟摇杆与掌机陀螺仪·UGC官方编辑器产品决策·热重载四档·API三键兼容·沙箱能力交集·加载冲突稳定仲裁·保底计数原子写入·退款世界回滚预案·数据布局AoS/SoA/AoSoA·SIMD标量黄金路径·批处理完整归档·调试设施六态·云串流端到端**
scripts/game_meaning_layer.py
                      **🔑可辨性优先于可播放性·声音事实非音效事件·脚步四段链条·楼上楼下独立字段·威胁声学角色拆分·步态暴露状态·成长五通道与delta_direction·沉默事实·能力解锁改变世界·聚焦生命周期19事件·多显示器拓扑·截图录屏能力清单·跨通道节拍偏移与beat_clock·等待四要素·APM天花板·刻意坏味清单·社群锚点三空间版本语义**
scripts/game_inference.py
                      **🔑玩家据此以为会发生什么·常识违反是资产·Wood_Barrel_01不可燃但Wood_Crate可燃·常识测试矩阵·可学习线索vs静默陷阱·符号四态verdict·OOB是原版地图的一部分·边界厚度测距离非布尔·OOB美学·通道权威性按状态机·primary/indicative/thematic/undefined·动画命中判定未中＝游戏撒谎·伪因果四档·复现随机接口非只分布·designed_illusion·可解释性四级counterfactual·设备老化与校准·save_lineage·source_kind六种**
scripts/game_relation.py
                      **🔑关系状态·系统之间的时间接缝·离开前最后120秒·AFK六态·回归奖励是承诺测试样本·未完成存档的尊严·断点与最后动作·第N次四层计数×语境标签·跳过是控制权迁移非删内容·第50次台词穷尽衰减冷却伪穷尽·幽灵六级·门后没有模型≠没有承诺·第一次三层不可再生·self_rules·clumsiness_matrix·共谋不揭穿·nostalgia_probe**
scripts/game_identity.py
                      **🔑名称只是标签身份才是契约·同形异质六类·ID四层·背包去重消灭玩家的剑·整理背包改变结果·observed_slot_history·个人操作方言是动作短语非键位表·intent与command分离·缓冲宽严双向陷阱·无效输入的副作用·意外组合八接缝·三级分类·惯例层与原版层并存·Tank/现代双方案·挂机是契约·输得好看·错过的稀缺性本身就是内容·seed_contract·prompt_binding·explanation_audience**
scripts/game_social.py
                      **🔑四条外部依赖链·老玩家教的是旧版本·知识声明三层可分别质疑·假可玩性界面先承诺系统再拒绝·placebo·试玩残留·教程临时能力·玩家第二职业依赖可观测裂缝·四类接口·版本地层DAG非时间轴·标题相同≠构件相同·地区版不能写日版总是更难·仪式性浪费·个性化四个破绽接缝·苦行边界与多通道验证·metagame_entry·absent_social_clock·player_evidence·player_agency·aesthetic_stratum**
（第五十五轮新增 --import：真实捕获导入）
                      **🔑闭合第五十一轮留下的第三项把真实捕获接进流水线·核心设计是真实捕获没有真值所以不生成ground_truth.json且verify被明确拒绝退出码1·把合成自测与真实取证分成两条路径从机制上挡住用自测冒充取证·真实导入必须先ffprobe探测fps时长音轨缺ffprobe直接报错绝不用猜的fps切段·探测失败才用显式默认25并标fps_guessed不静默·实测用ffmpeg-testsrc2生成的91KB真实编码素材走真实解码路径·探测fps15时长4秒音轨有-capture60帧-join74个时间点-观测段60-T_min为null-verify拒绝退出码1·管道已通等待真实素材一旦有现场视频喂进来segment_frames才是证据**
scripts/build_manifest_spdx.py
                      **🔑落地前先打开规范原文核对结果推翻修正了三处·第五十一轮记的18个字段实为9自有加10继承共19且必填只有buildType-spdxId-creationInfo三个·buildId官方原文是locally-unique-identifier不是全局唯一须自加externalIdentifier全局层·buildStartTime与buildEndTime官方明说可省略以简化可复现构建且3点1已弃用·我们一直在记构建时间戳这反而损害可复现所以脚本默认不写只有显式with-timestamps才写·buildType官方措辞是hint只是提示不能当权威真相·前八轮完全没记的是Build-profile要求至少3个LifecycleScopedRelationship·hasInput-hasOutput-invokedBy且scope必须为build·只有字段没有关系不是Build只是一张属性表·映射发现内部manifest里invokedBy完全没有对应字段·端到端实测合规片段通过删掉hasOutput被拦加时间戳虽过但警告可复现性下降**
（…第一百零四轮G398；第一百零五轮 **落盘带mode与双层并存断言-G399**）
                      **🔑第一百零五轮执行第一百零四轮末尾留下的两条指引·一把落盘改成mode制表符path-使哪个文件的哪个权限不对也离线可答·二加门禁断言G397与G398并存-防将来有人删掉其中一个·两条都做了-各四方向与五方向破坏实测·其中一次破坏第一次没做成-回看才发现-已如实记录·落盘改为mode制表符path·断言输出-444515de4b42-266行-指纹fb8e6fee一致-mode-100755·排序仍只按path-不按mode与path-因为台账的odd-paths-sha也是按path排的·两者必须同源-若改成按mode与path排-多种异常mode时指纹会与台账对不上·新增内部自洽校验-落盘mode必须在台账odd-modes内·破坏实测-落盘塞入100644而台账只记100755-报落盘与台账自相矛盾·这条回答了哪个文件的哪个权限不对-同时堵住落盘塞进正常文件却无人发现·它防的是落盘与台账互相打脸-不是两者一起错-后者仍由G397查远端兜底·G399在线层与离线层必须并存·判据一-两层都在自动门禁表-防被删除或移到人工门禁·判据二-两层kind都是hard-软警告会让层缺失被当提示·判据三-两层命令必须不同-相同命令等于同一实现-两层是假的·判据三是核心-assert-legacy-files与assert-legacy-dump必须是两条独立实现·若有人把G398的命令改成G397的-表面看两条门禁都在-实际只有一层·五方向破坏实测·删掉G397只留离线层-报不在自动门禁表·G398命令改成G397的-报两层命令相同-两层是假的·G397的kind改成soft-报kind等于soft·落盘塞入台账没记的mode-报自相矛盾·落盘还原成纯path旧格式-报266行缺少制表符·第五项第一次没做成-回看才发现·我cp了一份备份当旧格式-但那份备份是本轮新格式-备份是在改格式之后取的-测试跑出rc等于0·若当时就信了rc等于0-会误记为旧格式能兼容通过·重造一份真正的纯path文件后才抓到缺少制表符·与第一百轮自测测了抄的那份同源-破坏没做成时-结果看起来是通过了-而实际是没测到·一个实现坑-GATES元组索引·第一版把g下标2当kind-实际是cmd·GATES元组结构-gid-desc-cmd-kind-files-kind是下标3·这类错误有个明显特征-报出来的值类型不对-应是一个词hard或soft-却是一整个命令列表·看到就该回查索引·自动门禁145增至146-新增G399-人工250-实测通过26共146-阻断1仅G37缺输入-跳过119·verify-all现为31份清单643条未兑现0·仍不完备·一-G399只登记了一组G397与G398-G396在线与G398离线也存在同样的互补关系-尚未登记·二-判据三只比命令字符串-两条命令不同但实现同一函数查不出来·三-落盘不含该文件本应是什么mode-只记了实际mode-应该是100644仍要靠baseline-modes反推·四-BOTH-LAYERS里的为什么必须并存是人工写的理由-没有机制验证该理由与实现一致·五-五个历史commit权限仍未修正·后半补充-略·第一百零七轮-基准指纹-让消失与被修复可区分·解决的问题-某文件chmod加x-基准松了-历史遗留集体消失-而asserthistory Known只报台账过期-不解释为什么突然没了·消失与被修复必须能被区分-否则遗留归零这个最令人安心的结果恰是最危险的假象·两条判据-一台账必须有baseline-fp-缺失则拒绝给结论不静默·二-台账指纹等于当前索引基准指纹-不等则阻断·基准漂移三分支-减少等于消失基准松了·增加等于基准收紧·数量不变也要报-判定基准本身变了-结论含义已不同·第三分支最容易漏-数量没变时看起来一切正常-但依据已不同·三方向破坏实测-基准松了-删baseline-fp-删台账-全部拦截·自动门禁147-新增G400-人工250-实测通过27斜杠共147·33份清单735条声明·仍不完备-一-G400只做事后发现-不主动防止基准被改-真要防需让audit-history之外的入口不许改索引mode-未做·二-基准指纹不记什么时候变的-不知哪一轮为什么变·三-台账不记历史基准指纹列表-只留最新一个·四-G400与G397G398之间没有登记互补关系·五-五个历史commit权限仍未修正·119项跳过不变-发现仍靠人工·第一百零八轮-基准变更史-让哪一轮变的为什么变离线可答·baseline-fp-history首次也建-否则没变更过与变更史被删无法区分·上限BASELINE-FP-HISTORY-MAX等于20-保最早1条加最近19条·实测抓到解析bug-一百零七被算成100-百后零七不在字典取0·该bug不报错-只是悄悄记错轮次-而且错成看起来完全合理的100·看起来合理的错误数字比明显错误的更难发现·改为逐字符累计·已回填真实轮次108并在条目写明曾被误记·G401三条判据-一-history必须存在·二-每条含五字段·三-链条闭合-每条from-fp等于上一条to-fp·三b-末条to-fp等于台账当前指纹·链条闭合最关键-只验形状合法时-删掉中间任意一条仍全通过-历史就变编的·四方向破坏实测-删字段-删legacy-to-追加deadbeef断链-末条改值-全部拦截·自动门禁148-新增G401-人工250-实测通过28斜杠共148·34份清单784条声明·仍不完备-一-round取自文档最大轮次不是变更真正时刻-一轮内多次变更会记成同一轮·二-变更史没有独立门禁防整段手工替换-G401只验链条闭合-整段重造且自洽验不出-与89轮同源自证循环同源·三-at不参与比对-什么时候实际只有轮次·四-上限20条丢弃中间历史-保留最早加最近是人工选择·五-五个历史commit权限仍未修正·119项跳过不变-发现仍靠人工·第一百零九轮-变更史镜像-让整段替换须同时改两处·先证明108轮②是真的-构造自洽替身变更史-链条闭合seq连续末条指纹相符-G401确实rc=0放过-G402rc=1抓到-不是推演是实测·解法-镜像放指纹不放副本-复制会形成同源自证-与G398同源自证循环同源·放指纹-改任一处立刻不符-且不随历史增长·三判据-镜像存在可解析-history-sha相符-len与baseline-fp相符-不得有多余键·顺带补108轮①-单调序号-实测当场复现-两条变更都标第108轮-因round取自文档最大轮次-新增seq全局1到n与seq-in-round-G401断言seq等于下标加1-删中间一条后重编号会被抓·真实工作流验证-新增镜像文件后索引282变283-G400正确报漂移阻断-按规程重跑audit-history重建台账加镜像-三条全绿-这不是误报是基准真变了·四方向破坏-整段替换G401rc0对G402rc1-同时改两处rc0已知边界-删镜像-塞多余键·自动门禁149-新增G402-人工250-实测通过29斜杠共149·35份清单836条声明·最关键局限-同时改两处时G402也会通过rc=0-所以镜像不防蓄意同时改两处-只防改一处忘一处即误操作与半途而废的篡改·真防蓄意需第三方不可写存储-远端独立分支或签名-代价高未采用·其它未做-一-镜像不含变更史内容·二-镜像只由audit-history写-其它入口改变更史不会同步镜像未设门禁禁止·三-第三组G401G402已登记但G400仍无互补层·四-五个历史commit权限仍未修正·119项跳过不变-发现仍靠人工**