# rc 合法域 `(0, 1)` 字面量 —— **语义豁免清单**
# 🔑 格式：文件名: NN轮: 豁免理由    （🔴 必须写明理由，且 ≥ 10 字符）
# 🔑 第九十六轮：理由**必须以 NN轮 开头** —— 让批准时间可追溯。
# 🔴 只用于**语义确实不同**的 `(0,1)`，不得用于掩盖真正的分裂。
#
# 🔑 第九十四轮规律：字面量相同 ≠ 语义相同。
#    astgrep 的 rc 中 `1 = 有命中（lint 语义）`，**不是**"门禁阻断"。
#    ✅ 正确做法是登记豁免并写明语义差异，🔴 而不是强行合并常量。
ast_query.py: 94轮: astgrep 的 rc：1 = 有命中（lint 语义），**不是**门禁阻断 —— 语义不同，不得合并
