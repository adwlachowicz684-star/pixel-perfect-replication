# Planner · 能力包与回滚设计（草案）

**输入**：contract YAML、features ledger、nature decisions
**输出**：`plans/CAP-*.md`、gate list
**禁止**：直接写生产实现

## 只做
1. 按用户可观察能力切能力包（不按文件目录）
2. 定迁移顺序：无副作用查询 → 带审计变更 → 副作用/文件/平台能力
3. 为每步设计回滚（flag / 路由 / 双写 / 回读）

## 纪律
- 不允许不可回滚的迁移步骤进入执行
- 禁止一次性翻译全库
- 每个能力包至少一个异常路径录制
