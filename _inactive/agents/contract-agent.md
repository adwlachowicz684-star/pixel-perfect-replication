# Contract · 契约抽取（草案）

**输入**：原版命令/事件/DTO、新版对应位置
**输出**：`contracts/*.yaml`、覆盖率报告
**禁止**：改变原版契约

## 只做
1. 抽取命令名、参数（含可选性、编码、默认值）、结果变体、副作用、幂等
2. 抽取 DTO 字段与改名映射
3. 跑四类检查（C1-C4）+ 故障注入 F1-F4

## 纪律
- 排除注入参数（app/state/window）
- 结构体按大括号配对截取，**不用 `[^}]*`**
- 脚本第一次全绿时必须故障注入验证
